# brandnana

A brand-intelligence service built on workbooks: agents read a company's
website and produce a complete brand book — palette, fonts, voice, lifestyle
imagery — as a single workbook (`<slug>.html`).

**Status:** running privately; the source tree is not yet published to this
repo (a secrets/asset scrub pass is pending). The example is listed here so
the gallery is honest about what exists.
