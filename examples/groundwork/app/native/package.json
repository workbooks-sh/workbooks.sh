{
  "name": "groundskeeper-shell",
  "private": true,
  "version": "0.1.0",
  "description": "Native iOS shell around the groundskeeper workbook (wb-3ojf.5)",
  "scripts": {
    "sync": "mkdir -p www && cp -f ../groundskeeper.html www/index.html && cap sync ios"
  },
  "dependencies": {
    "@capacitor/cli": "^8.4.0",
    "@capacitor/core": "^8.4.0",
    "@capacitor/ios": "^8.4.0"
  }
}
