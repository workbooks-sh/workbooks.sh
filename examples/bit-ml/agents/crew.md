# crew — the bit.ml newsroom manifest

Read by `Workbooks.Keeper.Crew` (`WB_CREW_DEF`). One entry per agent.

- **desk** — DEF: `/data/agents/desk.html`, INTERVAL: 45m
- **moss** — DEF: `/data/agents/researcher.html`, INTERVAL: 15m
- **wren** — DEF: `/data/agents/writer.html`, INTERVAL: 15m
- **hale** — DEF: `/data/agents/editor.html`, INTERVAL: 20m
