{"id":"wb-stat-card","sys":"You are an expert front-end engineer. Output ONLY a single complete code block, no prose.","user":"Write a Lit 3 web component `wb-stat-card` (a custom element). Reactive properties: `label` (string), `value` (string), `trend` (number, percent). Use Shadow DOM. Style it ENTIRELY from CSS custom properties for theming: --wb-surface, --wb-text, --wb-accent, --wb-radius, --wb-font; provide sensible fallbacks. Show the trend in green when positive, red when negative, with a ▲/▼ glyph. Register the element."}
{"id":"wb-data-table","sys":"You are an expert front-end engineer. Output ONLY a single complete code block, no prose.","user":"Write a Lit 3 web component `wb-data-table`. It has a `rows` property (array of plain objects) and an optional `columns` property (array of keys). If columns is omitted, infer them from the first row. Render an HTML table in Shadow DOM. Clicking a column header sorts by that column, toggling asc/desc. Theme from --wb-surface/--wb-text/--wb-border CSS variables. Register the element."}
{"id":"compose-dashboard","sys":"You compose UI by REACHING FOR existing pre-built `work-*` web components — you do NOT write new component source. Output ONLY one HTML code block.","user":"Given these existing custom elements: <work-grid cols>, <work-card title>, <work-stat label value trend>, <work-chart type from>, <work-table from>. Compose a single-file HTML dashboard fragment for a sales dashboard: a 3-column grid with two stat cards (Revenue $1.2M +12%, Orders 3,401 -3%), a line chart bound to data source 'revenue_by_month', and a table bound to 'top_products'. Use the elements as-is via attributes."}
{"id":"toolkit-csv","sys":"You are an expert Python engineer. Output ONLY a single complete code block, no prose.","user":"Write a Python function `summarize_csv(csv_text: str) -> str` that parses CSV text (with a header row), and returns a JSON string mapping each numeric column name to {min, max, mean, count}. Ignore non-numeric columns. Use only the standard library. Handle empty input gracefully."}
{"id":"calib-intervals","sys":"You are an expert engineer. Output ONLY a single complete code block, no prose.","user":"Write a Python function `merge(intervals: list[list[int]]) -> list[list[int]]` that merges all overlapping intervals and returns them sorted by start. Example: merge([[1,3],[2,6],[8,10],[15,18]]) == [[1,6],[8,10],[15,18]]."}
{"id":"calib-debounce","sys":"You are an expert engineer. Output ONLY a single complete code block, no prose.","user":"Write a JavaScript `debounce(fn, wait)` higher-order function that returns a debounced version of fn: it delays invoking fn until `wait` ms have elapsed since the last call, passes through the latest arguments and `this`, and exposes a `.cancel()` method."}
