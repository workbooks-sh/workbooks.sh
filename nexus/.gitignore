
priv/jsdom.js
priv/eval-host.wasm
priv/measure.wasm

# Worktop (Burrito) desktop-deploy build output — the wrapped self-contained host binary,
# regenerated by `mix release worktop` / `work deploy desktop`, never committed.
/burrito_out/

# relocated compiler-infra artifacts (moved in from runtime/, regenerated not committed)
/compilers-dist/
build/tools/wasm-tools.wasm
build/tools/*.wasm
/build/commands/*.d/
/build/commands/registry.json

# relocated compiler BUILD output (regenerated, never committed)
/build/build/
/build/jsdom/
/priv/*.wasm
/kits/*.wasm

# vendored compiler bundles (fetched/built by each lane's build.sh, shipped via the compilers
# package — NOT committed to git): svelte compiler, solid babel bundle, python interpreter.
/compilers/*/vendor/
/compilers/python/*.wasm

# swarm demo session history (SQLite, generated)
examples/swarm/sessions.db
sessions.db

# swarm demo generated data
examples/swarm/sessions.db
examples/swarm/*.dets

# test262 full clone (vendored curated slice is committed under test/conformance/test262)
.test262/
