#+TITLE: lander-live demo engine — deployment
#+DEPLOY_TARGET: fly
#+DEPLOY_APP: wb-lander-live
#+DEPLOY_REGION: sjc
#+DEPLOY_PORT: 4000
#+DEPLOY_MEMORY: 2048   # MB — BEAM+wasmtime OOMs under ~1GB (friction #7)
# Secret NAMES this deployment needs (values: `wb deploy secrets set KEY=VAL`):
#+DEPLOY_SECRETS: OPENROUTER_API_KEY
# (POSTHOG_API_KEY is OPTIONAL — analytics phase; keeper degrades to clarity edits without it)
# Toolkits the deploy SHIPS to the engine (id:dir, space-separated). Write a
# toolkit, `wb deploy apply`, done — the kit launches the toolkit to the runtime.
#+DEPLOY_TOOLKITS: living-lander:./living-lander

# The lander-live proof (bd wb-5vm): one small fly machine running the runtime
# image. It serves the lander (a workbook) over PublicWeb, runs lander-keeper
# (living-lander/agent/keeper.org) hourly, and exposes /rcp/changes — the page's real git history —
# to the page's own inspector. Bring up:
#
#   cd web/lander-wb/keeper
#   wb deploy secrets set OPENROUTER_API_KEY=sk-or-…
#   wb deploy apply
#   wb publish init        # PUBLISH_TARGET: self-hosted → the fly URL
#   wb publish apply ../dist/workbooks.html
#
# Keeper model: deepseek/deepseek-v4-pro (vision + computer use — it screenshots
# the page to verify its own edits). Loop trigger v1: hourly cron hitting
# POST /api/run with the keeper def; see bd wb-5vm sub-issues.
