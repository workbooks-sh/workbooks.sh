#+TITLE: sections — the page/section map (check before adding, update after)

The anti-sprawl law: ONE place per idea. Before any ADD, find your idea here.
Same intent already mapped → improve that section instead. After any ADD: add
the row here in the same commit. Source of truth for page order = the
`sections/NN-` filename numbers.

* page.tpl.html (human shell — HANDS OFF)
- hero — the demo + the pitch ("a website that builds itself", ecosystem ladder)
- 01 · the stack — #layers: the three layers as color-lit cards
- 02 · built for — #who: person → team → world ladder, three cards
- 03 · get workbooks — #get: desktop download + github for developers
- timeline panel / footer

* sections/ (yours — starts EMPTY; numbering from 04; 99-faq is last)
- (nothing yet — genesis was 2026-06-10; the board has the roadmap)

* blog/ (fresh — every post must hit the query gate in skills/seo.org)
- (nothing yet)

* rem/ — the dream journal (system-written; never edit)
