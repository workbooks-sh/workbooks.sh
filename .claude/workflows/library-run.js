export const meta = {
  name: 'library-run',
  description: 'THE library run: all 71 remaining deep-dive lessons through research → write → adversarial review',
  phases: [
    { title: 'Research', detail: 'facts + outline per lesson' },
    { title: 'Write', detail: 'page + eyes-closed audio script' },
    { title: 'Review', detail: 'adversarial gate + fixes' },
  ],
}

const LESSONS = [{"slug": "signatures", "parent": "workbook", "title": "signatures", "dek": "Make provenance travel with the bytes: sign a workbook with a self-certifying did:key (Ed25519 over canonical JSON, embedded in the file) and verify both the signature and that the artifact hasn't been touched since.", "evidence": "runtime/host/manifest.ex (embedded <script type=\"application/workbooks-c2pa+json\"> manifest, two-check verify: signature + asset sha256, canonical-JSON contract); cli/src/local.rs sign/verify ('byte-compatible with Workbooks.Manifest'); runtime/host/bundle.ex ship() signing on egress; the wbx sign/verify story in web/learn/workbook.html's terminal block", "color": "#a8d4f0", "nn": "01·3"}, {"slug": "specs", "parent": "workbook", "title": "specs", "dek": "How a workbook identifies and describes itself from the inside — the inline org spec and embedded source markers that make the format filename-independent and self-explaining.", "evidence": "runtime/host/workbook.ex (format identified by <script id=\"workbook-spec\"> and <script id=\"workbook-org\">, never the filename); web/learn/hello-workbook.html (a live workbook that narrates its own anatomy); web/learn/workbook.html itself carries a workbook-spec block at the bottom of its source", "color": "#a8d4f0", "nn": "01·4"}, {"slug": "git", "parent": "workbook", "title": "git", "dek": "The artifact is version control native: one file, real diffs, the whole project's history in the repo you already have.", "evidence": "discover from the codebase: search runtime/host, cli/src, runtime/kernel for this concept", "color": "#a8d4f0", "nn": "01·5"}, {"slug": "sharing", "parent": "workbook", "title": "sharing", "dek": "Hand a workbook to anyone with cryptographic identity attached: did:key signatures, single authentication, and what the recipient can verify without an account.", "evidence": "discover from the codebase: search runtime/host, cli/src, runtime/kernel for this concept", "color": "#a8d4f0", "nn": "01·6"}, {"slug": "postures", "parent": "workbook", "title": "postures", "dek": "Every workbook route has a security stance — public, gated_data, or gated_route — and the anti-leak invariant that decides what may be baked into a static file versus served only behind the auth ladder.", "evidence": "runtime/host/access.ex (three postures + enforce, the anti-leak invariant), runtime/host/auth.ex (five-rung auth ladder: allowlist, desktop token, WB_PUBLIC_BEARER, Guardian JWT, dev header)", "color": "#a8d4f0", "nn": "01·7"}, {"slug": "publishing", "parent": "workbook", "title": "publishing", "dek": "Publish is distribution, not deploy: publish.org declares a surface (cloudflare-pages, gh-pages, self-hosted) with init/validate/apply convergence, ships only ASSEMBLED workbooks (bundle first), prints custom-domain URLs via PUBLISH_DOMAIN, and stays a separate axis from standing up the engine.", "evidence": "cli/src/local.rs:336-443 (publish.org template, three targets, PUBLISH_DOMAIN, shippable_html bundle-first guard, wrangler/git/HTTP PUT paths); cli/TAXONOMY.md §3+§5 (publish vs deploy collision resolved, lifecycle spine); runtime/host/publish.ex + runtime/host/publish/config.ex (runtime-side counterpart)", "color": "#a8d4f0", "nn": "01·8"}, {"slug": "the-dock", "parent": "nexus", "title": "the dock", "dek": "The reader understands the Dock as a typed capability membrane: the host hands a workbook only the functions its grants permit, so a component asking for an ungranted power fails to even instantiate.", "evidence": "runtime/host/instance/imports.ex (the `workbooks:engine` imports map, gating 'by construction'); runtime/host/rust_dock.ex and js_dock.ex (the same membrane for compiled Rust/JS core modules); runtime/host/secret_broker.ex (sign-but-never-read secrets — the host holds the key, the guest only ever sees the signature); brokers for exec/queue/tcp/udp/tls (exec_broker.ex, queue_broker.ex, tcp_broker.ex). Parent page says 'grants, not inheritance' in one sentence — this page is the mechanism.", "color": "#aee5c2", "nn": "02·1"}, {"slug": "capabilities", "parent": "nexus", "title": "capabilities", "dek": "The reader can read and reason about a Policy profile: the four standard profiles, how each maps to a memory ceiling, a wall-clock CPU trap, and a capability list — and why a minimal profile literally has no network stack.", "evidence": "runtime/host/policy.ex — the @profiles table (minimal/network/posix/compute with memory caps 64–256MB, cap lists, 5–60s timeouts), store_limits/1 (a component growing past its cap traps), timeout/1 (runaway calls trapped at the boundary, BEAM stays responsive), allow_http?/1 (the single switch that removes wasi:http, socket inheritance, AND DNS for non-net profiles — security finding wb-sec #7 documented in-module).", "color": "#aee5c2", "nn": "02·2"}, {"slug": "sandboxes", "parent": "nexus", "title": "sandboxes", "dek": "The reader can trace a docked workbook through its whole state machine — created → active → suspended → frozen → archived — and sees why 'freeze' is just 'stop the process, keep the SQLite file', no VM snapshot needed.", "evidence": "runtime/host/lifecycle.ex (the @transitions map, idle auto-demotion policy: 15 min → suspend, 24 h → freeze; clone_for/3 template-clone-per-tenant; freeze = copy the VFS file to cold storage; litestream replication noted); runtime/host/control_plane.ex (the SQLite session registry — instances table with id/tenant/state/vfs_path; 'SQLite is the control plane, the data plane is each Instance's VFS'); runtime/host/instance/supervisor.ex.", "color": "#aee5c2", "nn": "02·3"}, {"slug": "keepers", "parent": "nexus", "title": "keepers", "dek": "The reader learns how a workbook keeps working with nobody watching: keeper workers ticking agent defs on intervals, singleton vs crew scheduling, lifecycle specs that decide what each tick is, and schedules declared in the org source.", "evidence": "runtime/host/keeper.ex (env contract: WB_KEEPER_DEF / WB_KEEPER_INTERVAL_MS / plan-vs-edit modes; singleton facade over keeper/worker.ex; keeper/crew.ex one-worker-per-agent); runtime/host/keeper/lifecycle.ex (optional state-machine spec consulted per tick); runtime/host/workflow.ex (workflows parsed from org with their schedules surfaced); the live proof is the lander fleet (examples/living-lander) whose commits ARE the public changelog via the keeper's tenant repo.", "color": "#aee5c2", "nn": "02·4"}, {"slug": "planes", "parent": "nexus", "title": "planes", "dek": "The reader understands why one Nexus runs two separate HTTP surfaces — an authed control plane and an anonymous GET-only content plane — and how a single BEAM node serves many custom domains via an ETS-backed SNI registry, no reverse proxy.", "evidence": "runtime/host/public_web.ex (hard isolation rules: no Auth plug, GET-only, no Dock/commands/secrets routes exist; the 'served by workbooks' marker; /_changes and /_activity feeds); runtime/host/web.ex (the authed control plane, WB_WEB=1); runtime/host/application.ex web/0 (control on PORT 4000, public on PUBLIC_PORT 4001 + PUBLIC_TLS_PORT 4443 with sni_fun); runtime/host/domains.ex (host→app registry; unknown SNI host gets no cert so the handshake can't complete — admission gate). Deployed shape: public plane exposed, control plane internal+locked (lander-live).", "color": "#aee5c2", "nn": "02·5"}, {"slug": "deployments", "parent": "nexus", "title": "deployments", "dek": "The reader stands up a Nexus both ways — a cloud-identical krunvm microVM on their laptop and a Fly deploy — and understands deployment.org as inert declarative config where moving to the cloud is editing one property.", "evidence": "runtime/host/deploy/config.ex (deployment.org parsed as a hand-rolled :PROPERTIES: scan, never executed; validate/1 encodes the honest combos — s3 needs bucket+endpoint, postgres/s3 secrets live in env never the file); runtime/host/deploy/machine.ex (local place = krunvm microVM: case-sensitive APFS volume preflight, create/start/delete contract, discovery file); runtime/host/deploy/backend.ex (provider seam: adding a cloud = drop a directory with bootstrap.sh filling five hooks); cli/deploy-kit/deployments/local.org + cloud-saas.org and cli/deploy-kit/providers/fly/bootstrap.sh (the shipped examples). Backs the parent's 'dev is prod' section with the actual verbs: init / validate / apply / status.", "color": "#aee5c2", "nn": "02·6"}, {"slug": "telemetry", "parent": "nexus", "title": "telemetry", "dek": "One event shape across the whole engine — what gets recorded, how agents and the autopoet read it, and why one grammar of signal beats a zoo of logs.", "evidence": "discover from the codebase: search runtime/host, cli/src, runtime/kernel for this concept", "color": "#aee5c2", "nn": "02·7"}, {"slug": "gitops", "parent": "nexus", "title": "gitops", "dek": "The engine syncs to git: tenant repos, commits as the public changelog, and push-to-live publishing.", "evidence": "discover from the codebase: search runtime/host, cli/src, runtime/kernel for this concept", "color": "#aee5c2", "nn": "02·8"}, {"slug": "hot-swap", "parent": "nexus", "title": "hot swap", "dek": "Loaded artifacts replace themselves live — a git change lands, the new toolkit/def swaps in, nothing reboots.", "evidence": "discover from the codebase: search runtime/host, cli/src, runtime/kernel for this concept", "color": "#aee5c2", "nn": "02·9"}, {"slug": "networking", "parent": "nexus", "title": "networking", "dek": "No ambient network, ever: how brokered TCP/UDP/TLS and engine-held HTTP give workbooks exactly the connections they were granted.", "evidence": "discover from the codebase: search runtime/host, cli/src, runtime/kernel for this concept", "color": "#aee5c2", "nn": "02·10"}, {"slug": "channels", "parent": "nexus", "title": "channels", "dek": "Messaging channels as runtime capabilities: credentials live host-side, adapters self-configure off env, and unknown senders are parked behind a pairing-code allowlist before anything reaches the inbox.", "evidence": "runtime/host/channels.ex (registry + dispatch + DM pairing policy, JSON/JSONL state under WB_DATA/channels/), runtime/host/channels/telegram.ex (official Bot API adapter, supervised long-poll GenServer, configured?/0 gating in Workbooks.Application)", "color": "#aee5c2", "nn": "02·11"}, {"slug": "secrets", "parent": "nexus", "title": "secrets", "dek": "Secrets-by-reference: variables marked secret are ref-only — agents substitute {{secret:KEY}} placeholders, the host injects real values only at egress, and the deploy kit stages declared secret names per deployment and delivers values through provider hooks without ever baking them into images or config.", "evidence": "runtime/host/vars.ex (ref-only model, :guest vs :host resolution, docs/SECRETS.org rule); cli/src/local.rs:445-504 (wbx var set --secret, {{var:}}/{{secret:}} refs, masked list); cli/src/deploy/mod.rs:17-21,185-274 (#+DEPLOY_SECRETS declared names, staged 0600 secrets.env, secrets set/list/unset/push); cli/deploy-kit/providers/_recipe.sh:25-46 (WB_SECRET_KEYS forwarding); cli/deploy-kit/providers/fly/bootstrap.sh:39-42 (provider_set_secrets hook)", "color": "#aee5c2", "nn": "02·12"}, {"slug": "browsing", "parent": "nexus", "title": "browsing", "dek": "The runtime's web capability: a free native browser by default, with fetch/crawl/search each routable to a swappable provider (Firecrawl, Exa, proxies) via config — no caller ever changes.", "evidence": "runtime/host/browse.ex (capability dispatcher), runtime/host/browse/provider.ex, native.ex, fetch.ex (127 LOC), crawl.ex, search.ex (152 LOC), extract.ex (151 LOC), firecrawl.ex", "color": "#aee5c2", "nn": "02·13"}, {"slug": "tokens", "parent": "nexus", "title": "tokens", "dek": "Credentials with a no-rotation discipline: first cloud apply generates a 256-bit WB_PUBLIC_BEARER once and persists it (rotation would 401 every live client), clients present it as WB_ENGINE_TOKEN, and WB_SIGNING_KEY pins a stable DID across redeploys so prior signatures and ledgers keep verifying.", "evidence": "cli/src/deploy/mod.rs:374-429 (ensure_cloud_bearer idempotent generation, OsRng token, persisted-in-secrets.env notice) + TEMPLATE_FLY security block (lines 570-587); runtime/host/auth.ex (WB_PUBLIC_BEARER enforcement); cli/deploy-kit/providers/_recipe.sh:27-32 (generated secrets persisted, never rotated per-apply); cli/deploy-kit/storage.env.example:10-16 (WB_SIGNING_KEY stable-DID requirement)", "color": "#aee5c2", "nn": "02·14"}, {"slug": "changelogs", "parent": "nexus", "title": "changelogs", "dek": "Published apps serve their own history: an anonymous read-only /_changes feed (git log + agent status, public-plane rules) that the page itself consumes — classifying who made each commit, collapsing quiet human groups, and narrating the work live so visitors watch people and agents build together.", "evidence": "runtime/host/public_web.ex (get /_changes — git log_entries + keeper status, singleton vs crew shapes, plane rules), examples/living-lander/app/src/lib/stores.js (the watch() loop, classify()/describeCommit, one-engine feed subscribers, raw.githubusercontent mirror reads), runtime/host/git.ex", "color": "#aee5c2", "nn": "02·15"}, {"slug": "imports", "parent": "toolkit", "title": "imports", "dek": "Anything → toolkit: how wbx toolkit import sniffs nine source shapes (Claude skills, MCP configs, OpenAPI specs, npm/pip CLIs, cursor rules, AGENTS.md…), mechanically translates docs to org, and scaffolds a real manifest + skill tree — with carried scripts honestly flagged as untrusted.", "evidence": "cli/src/import.rs (Kind enum with 9 detected shapes, detect() shape-sniffing, md_to_org() the 'honest 90%' translator, write_scaffold(), per-kind parsers incl. import_mcp_server synthesizing launch scripts so the audit can judge them, import_openapi making a skill per operation); stage 2 auto-runs via crate::audit::audit_static at the end of import()", "color": "#f3c5a3", "nn": "03·1"}, {"slug": "audits", "parent": "toolkit", "title": "audits", "dek": "Ready, convertible, or blocked: how the static dependency audit classifies every carried script against the wasm lanes, writes findings INTO the toolkit's own manifest, and emits a checkbox fix-up plan an agent can work through to done — with --fix closing the loop as push → build → verify.", "evidence": "cli/src/audit.rs (Class::{Ready,Convertible,Blocked}, classify_interpreter/classify_binary truth tables — jq ready, curl→Dock, python blocked, docker blocked; worst-of roll-up; write_into_manifest 'the artifact carries its own audit'; fixup_plan() generating ** TODO [0/n] with per-finding recipe() steps; audit() --fix running toolkit_push/build/verify)", "color": "#f3c5a3", "nn": "03·2"}, {"slug": "lanes", "parent": "toolkit", "title": "lanes", "dek": "The crux bet of the toolkit layer: C, Rust, Zig, Go, and JS compilers that are themselves compiled to WebAssembly, so untrusted source is built entirely inside the sandbox — c4, mrustc-to-C, zig1's C backend, yaegi interpretation, and QuickJS embedding, each a different trick to dodge native execution.", "evidence": "runtime/compilers/README.org (zero-native-execution thesis); per-lane manifests: c/manifest.org (c4, compile-and-run), rust/manifest.org (mrustc → C → clang lane, e2e proof), zig/manifest.org (zig1.wasm stage1, compile-to-c), go/manifest.org (yaegi interpret-in-sandbox, wbgosrc custom section), js/manifest.org (QuickJS-ng + harness.c); rust/CRATES-COVERAGE.org + PROC-MACROS-PLAN.org for the honest frontier", "color": "#f3c5a3", "nn": "03·3"}, {"slug": "promotion", "parent": "toolkit", "title": "promotion", "dek": "Session → workspace → registry: an agent writes a source file, build-inline registers it as a command in one step, and promote materializes it as a real source-owned toolkit — org owns the spec, wasm owns the artifact, and self-authoring never widens granted capabilities.", "evidence": "runtime/host/toolkits.ex (build_inline_text/3 'no manifest ceremony' write→build→content-address→register; promote_text/4 with reserved-name and language guards; lang_layout/2 per-language source layouts; promote_manifest/promote_skill generating the scaffold; the Library.store/Library.install registry rung named in the promote docstring); parent lesson's #compose section only gestures at this with 'one command'", "color": "#f3c5a3", "nn": "03·4"}, {"slug": "trust", "parent": "toolkit", "title": "trust", "dek": "What the signature actually binds: an Ed25519 did:key signature over the canonical manifest body ties the declared contract (EXEC/CAPS/BUILD_SRC) to an author identity, the build gate refuses any unsigned or tampered third-party toolkit, and the sandbox bounds damage regardless — two layers, distinct jobs.", "evidence": "runtime/host/toolkits.ex (sign_text/3 strip-then-sign canonicalization, manifest_provenance/1 verification, trust_checks/2, the supply-chain gate in build_text/3: 'REFUSED — third-party toolkit with invalid provenance'; #+TRUST first-party vs third-party, first-party location-trust rationale in the threat-model comment); Workbooks.Git.did/sign/verify_sig as the shared crypto", "color": "#f3c5a3", "nn": "03·5"}, {"slug": "verification", "parent": "toolkit", "title": "verification", "dek": "Verify and eval as the toolkit's quality gates: structural checks (manifest, skills, exec, caps, provenance), then bundled evals/*.org cases where an agent is run against a :TASK: with the toolkit's own manual injected and a judge model scores the result and tool trace against a :RUBRIC:.", "evidence": "runtime/host/toolkits.ex (verify_text/2 structural + exec_checks + cap_checks + trust_checks; eval_text/2 and run_eval_case — Tier 2 agent_judge_case with :TASK:/:RUBRIC:/:MAX_STEPS:, judge/4 PASS-FAIL scoring; honest disabling of native :role eval per wb-9ja); toolkits/EVALS.org (the eval-case format); toolkits/toolkit-forge/skills/verify-toolkit.org", "color": "#f3c5a3", "nn": "03·6"}, {"slug": "commands", "parent": "toolkit", "title": "commands", "dek": "A command is a CLI converted to sandboxed WASM — stdin in, stdout out, invoked by name through the Dock — and guests exec each other's commands only through a default-deny broker with structural argv and bounded nesting.", "evidence": "runtime/host/command_registry.ex (1048 LOC: 32+ wasm commands, jq/grep/oql built-ins, command-vs-toolkit relationship), runtime/host/exec_broker.ex (default-deny, per-instance command allowlist, no shell-string injection, depth-limited nesting, audited denials)", "color": "#f3c5a3", "nn": "03·7"}, {"slug": "syntax", "parent": "org", "title": "syntax", "dek": "Learn the whole grammar in one sitting — headlines, tags, TODO states, drawers, timestamps, source blocks — by drilling each construct against the kernel's actual parse output, with side-by-side markdown comparisons showing exactly what structure markdown throws away.", "evidence": "runtime/kernel/src/lib.rs parse_headlines returns JSON rows (level/title/state/tags/props/scheduled/deadline) — perfect for paste-org-see-JSON drills; runtime/kernel/src/browser_abi.rs + Cargo.toml `browser` feature exist explicitly 'for the learn-site notebooks' (oql_parse_headlines/oql_render exported to wasm32-unknown-unknown), so the drills can run live in-page; the parent's own easter egg (web/learn/org.html lines 30-41) is a valid org comment to dissect; web/learn/org.html#why-not covers the markdown argument at concept level only — this goes hands-on.", "color": "#f2ddb0", "nn": "04·1"}, {"slug": "tangling", "parent": "org", "title": "tangling", "dek": "Literate programming, weaponized: how source blocks plus six header args (:deps :uses :in :out :persist :dir) compile a document into a WIT-world-shaped build plan — components, capability imports, and a DAG of :out→:in edges.", "evidence": "runtime/kernel/src/lib.rs tangle_plan/build_world/parse_args (the six header args, including Motoko-style :persist and :dir for real project dirs); sig_of() derives imports/exports/edges from header args alone; cli/src/kernel.rs `wb tangle` runs it locally with no runtime; cli/SPEC.md lists tangle as a kernel-local verb; runtime/examples/wb/nightly-digest.org is a three-language tangled workflow.", "color": "#f2ddb0", "nn": "04·2"}, {"slug": "drawers", "parent": "org", "title": "drawers", "dek": "How :PROPERTIES: drawers turn a headline into a typed record — the key-value mechanism behind toolkit manifests, agent identities, and the spec block inside every learn page.", "evidence": "runtime/kernel/src/lib.rs headline_of() reads hl.properties() and uppercases keys into a BTreeMap; runtime/toolkits/huniq/manifest.org (:ID:/:CLI_BIN:/:STATUS:/:SKILL_DIR:) and runtime/examples/agents/analyst.org (:MODEL:/:TOOLKITS:/:TYPE: — an agent's whole identity is a drawer); schedule_of() reads the :SCHEDULE: prop for cron; even web/learn/org.html's own <script id=\"workbook-spec\"> is a properties drawer driving the page.", "color": "#f2ddb0", "nn": "04·3"}, {"slug": "timestamps", "parent": "org", "title": "timestamps", "dek": "The full date grammar — active <…> vs inactive […], SCHEDULED vs DEADLINE, +1d/.+/++ repeaters, and the :SCHEDULE: cron escape hatch — and how a timestamp on a headline becomes a workflow that actually runs every morning.", "evidence": "runtime/kernel/src/lib.rs parse_timestamp() implements the exact grammar (angle/square brackets, time tokens, three repeater prefixes) and Timestamp::json() emits {at, repeat, active}; schedule_of() prefers :SCHEDULE: cron over SCHEDULED; runtime/examples/wb/nightly-digest.org carries `SCHEDULED: <2026-06-06 Sat 06:00 +1d>` on a :workflow: headline; runtime/host/workflow.ex surfaces the schedule in every run record.", "color": "#f2ddb0", "nn": "04·4"}, {"slug": "the-kernel", "parent": "org", "title": "the kernel", "dek": "Inside OQL — one 550-line pure Rust crate (parse, tangle, validate, render on orgize) that ships as three surfaces from the same source: a native rlib in the wb CLI, a WIT-typed wasm Component in the runtime and desktop, and a raw ptr/len build running in this very website.", "evidence": "runtime/kernel/src/lib.rs (pure string→string API, no host calls, no unsafe in the component path); runtime/kernel/wit/world.wit (the workbooks:oql world — six exports); runtime/kernel/src/browser_abi.rs (packed-u64 ABI for in-browser instantiation); runtime/kernel/Cargo.toml documents the cdylib+rlib split; consumers: cli/src/kernel.rs (native), runtime/host/oql.ex (wasm component), runtime/host/publish.ex + public_web.ex (render to HTML).", "color": "#f2ddb0", "nn": "04·5"}, {"slug": "todos", "parent": "org", "title": "to-dos", "dek": "The TODO grammar in full: states, transitions, what DONE actually records, and how state changes become history.", "evidence": "discover from the codebase: search runtime/host, cli/src, runtime/kernel for this concept", "color": "#f2ddb0", "nn": "04·6"}, {"slug": "tags", "parent": "org", "title": "tags", "dek": "Tags as structure: categorize headlines, drive queries, and carry meaning machines can filter on.", "evidence": "discover from the codebase: search runtime/host, cli/src, runtime/kernel for this concept", "color": "#f2ddb0", "nn": "04·7"}, {"slug": "claims", "parent": "org", "title": "claims", "dek": "Turn raw founder talk into an auditable working document: every load-bearing claim extracted as an org TODO item with an honesty status (CLAIMED → VALIDATING → KEPT/PARTIAL/UNKEPT/FUTURE/RETRACTED), every terminal status citing evidence — the gap between the spoken system and the shipped system becomes the backlog.", "evidence": "examples/groundwork/audits/spoken-thesis.org (the custom #+TODO keyword ladder, quote+evidence per claim, 91-min source audited), examples/groundwork/README.md (the recording → verbatim → claims → backlog pipeline), examples/groundwork/sources/ (untouched verbatim inputs the audit points back to)", "color": "#f2ddb0", "nn": "04·8"}, {"slug": "loops", "parent": "agents", "title": "loops", "dek": "What a single agent run actually is — the model→tools→append cycle, why every tool call is wall-clock bounded, and how a run leaves a fully readable trace.", "evidence": "runtime/host/agent.ex — loop/2 (LLM → tool_calls → exec → append → loop), exec_bounded/2 (150s per-tool kill), max_steps bound, done tool, events.org org-mode log + always-on _steps.jsonl telemetry; tool surface (shell/search/fetch/web_search/vfs_*) and the exec trust split (@exec_tools git/publish/image, host-brokered, no native exec — wb-9ja).", "color": "#9fc4e8", "nn": "05·1"}, {"slug": "authoring", "parent": "agents", "title": "authoring", "dek": "Write a complete agent definition in plain org — the :agent: node, the system-prompt heading, declared toolkits — and understand exactly how the runtime parses and runs it.", "evidence": "runtime/host/agent_def.ex — parse of the :agent: node (:ID:/:MODEL:/:TOOLKITS:/:TAGLINE:), the `** System prompt` heading rule (and the empty-prompt failure it guards against), toolkit auto-injection via Workbooks.Toolkits.injection_text; worked example: examples/living-lander/agent/agent.org (Waldo — mandate, ground rules, cadence, tooling, orientation budget).", "color": "#9fc4e8", "nn": "05·2"}, {"slug": "context-repo", "parent": "agents", "title": "context repo", "dek": "An agent's memory is a workbook — a git repo of plain files it reads, writes, and commits — not a vector store bolted on the side; the board, the canon, and the logs all live in one place anyone can diff.", "evidence": "examples/living-lander/agent/ — plan.org board protocol (TODO/NEXT/DOING/DONE states), context.org, design.org, design-gate.org, sections.org, skills/, rem/; runtime/host/agent.ex `search` tool ('no separate memory store; the files ARE the memory'); runtime/host/keeper.ex ('the keeper's commits ARE the public changelog', WB_TENANT repo as workdir).", "color": "#9fc4e8", "nn": "05·3"}, {"slug": "spawning", "parent": "agents", "title": "spawning", "dek": "How a run comes to exist and stays alive — supervised sessions you start with one call, then poll or stream while the agent works for minutes, surviving the caller that launched it.", "evidence": "runtime/host/agent_session.ex — DynamicSupervisor + Registry session start, the spawned run process, on_step fan-out to WS subscribers, GET /api/run/:id poll + /stream, WB_RUN env so a run can address itself; runtime/host/keeper/crew.ex worker_spec (one supervised Worker per crew member, staggered starts).", "color": "#9fc4e8", "nn": "05·4"}, {"slug": "orchestration", "parent": "agents", "title": "orchestration", "dek": "How standing agents keep a cadence — the keeper's tick, a lifecycle state machine declared in org, and a crew of workers sharing one board with claims as protocol, not locks.", "evidence": "runtime/host/keeper.ex (singleton facade, tick engine), runtime/host/keeper/worker.ex (instantiable core, crash-safety, NO-WORK fast-forward, catch-up scheduling), runtime/host/keeper/lifecycle.ex (org-declared state machine: :KIND:/:REPEAT:/:NEXT:/:MIN-INTERVAL:, position survives restarts), runtime/host/keeper/crew.ex (crew manifest, stagger, Crew.Gate concurrency cap, board claims as def-level protocol).", "color": "#9fc4e8", "nn": "05·5"}, {"slug": "human-in-the-loop", "parent": "agents", "title": "human in the loop", "dek": "Insert a real human checkpoint into a live run — a review canvas the agent opens, a commit the human makes, and a FIFO the agent polls mid-loop to pick the verdict up.", "evidence": "toolkits/ctk/manifest.org (CTK — 'a human-in-the-loop canvas', render EXEC shape) + skills/agent-review-loop.org, commit-connector.org, runtime-driving.org; runtime/host/agent_session.ex put_review/take_review (FIFO reviews, append-only review_log, durable per-run JSONL, live push to subscribers), WB_RUN + `wb ctk await $WB_RUN`.", "color": "#9fc4e8", "nn": "05·6"}, {"slug": "runtime-config", "parent": "agents", "title": "runtime config", "dek": "Every knob that shapes an agent without touching engine code — the env contract, org manifests, model and toolkit props, intervals, and the exec trust grant.", "evidence": "runtime/host/keeper.ex env contract (WB_KEEPER_DEF/INTERVAL_MS/MODE/CONTINUOUS, WB_TENANT, WB_LIFECYCLE_DEF), runtime/host/keeper/crew.ex (WB_CREW_DEF manifest + WB_CREW_STAGGER_MS/MAX_CONCURRENT, per-agent :DEF:/:LIFECYCLE:/:INTERVAL:), runtime/host/dreams.ex (WB_DREAM_MODEL, WB_DREAM_MIN_INTERVAL_MS), agent_def.ex :MODEL:/:TOOLKITS: props and run-time opts (max_steps, exec, model override).", "color": "#9fc4e8", "nn": "05·7"}, {"slug": "dreaming", "parent": "agents", "title": "dreaming", "dek": "What an agent does between runs — a sleep phase that digests telemetry, commits, and the backlog into a dream journal whose verdicts mechanically steer the next waking run.", "evidence": "runtime/host/dreams.ex (REM epic wb-2ku: gather → dream model → fixed headings tale/goals/blue sky/fears/verdicts/carry; daydreams as ephemeral ≤40-word musings, audit-gated full dreams); examples/living-lander/agent/agent.org board protocol step 1 (apply newest dream's * verdicts: pick up → NEXT, put down → TODO) and orientation step 0 (* carry as resume state); examples/living-lander/agent/rem/.", "color": "#9fc4e8", "nn": "05·8"}, {"slug": "evals", "parent": "agents", "title": "evals", "dek": "Judge agent output systematically: briefs in, judged runs out — the evaluation bench as a standing practice.", "evidence": "discover from the codebase: search runtime/host, cli/src, runtime/kernel for this concept", "color": "#9fc4e8", "nn": "05·9"}, {"slug": "the-ledger", "parent": "agents", "title": "the ledger", "dek": "From editable step logs to tamper-evident provenance: every agent run's _steps.jsonl is hash-chained over raw bytes and the head is signed with the tenant's Ed25519 did:key, making runs verifiable and attributable.", "evidence": "runtime/host/ledger.ex (seal/2 hash-chain h_i = sha256(h_{i-1} || raw_line_i), verify/1, Ed25519 head signing), runtime/host/did/x25519.ex, runtime/host/git.ex (tenant did:key material)", "color": "#9fc4e8", "nn": "05·10"}, {"slug": "fleets", "parent": "agents", "title": "fleets", "dek": "Run a multi-agent keeper from one declared org manifest: a worker per member with its own def, lifecycle, and namespaced state — staggered boots, a global concurrency gate that queues runs, and board claims as a def-level protocol the runtime deliberately does not enforce.", "evidence": "runtime/host/keeper/crew.ex (manifest format, stagger, WB_CREW_MAX_CONCURRENT, board-claims doctrine), runtime/host/keeper/crew/gate.ex, examples/bit-ml/agents/crew.org + writer.org/editor.org/researcher.org/desk.org, examples/bit-ml/board/pipeline.org, crew shape in runtime/host/public_web.ex (wb-wc0.2 {agents, wire} feed)", "color": "#9fc4e8", "nn": "05·11"}, {"slug": "rehearsals", "parent": "agents", "title": "rehearsals", "dek": "Self-demos for agents whose last hop can't be CI: drive the real persona and real tools through the production bridge with scripted human lines and no external service, writing a transcript that proves the whole conversation layer end-to-end.", "evidence": "runtime/host/groundskeeper/rehearsal.ex (persona from the def, every tool call through the real /gk router via Plug.Test, transcript writer), examples/groundwork/rehearsals/2026-06-11-045937.org (a real rehearsal transcript), examples/groundwork/README.md (rehearsals as a first-class directory of the method)", "color": "#9fc4e8", "nn": "05·12"}, {"slug": "modes", "parent": "wbx", "title": "modes", "dek": "How one binary serves two audiences — TTY detection, the --agent/--json switches, the {ok, verb, data, error} envelope, and the stable exit-code map an agent can branch on without parsing prose.", "evidence": "cli/src/mode.rs (detect(), classify() heuristic→exit codes 3/4/5/6/7, render_ok/render_err, verb_path(), pick() gated to human mode, next_hint() verb chaining); contract tests in cli/tests/mode.rs (envelope on success, code 4 on not-found, piped default = plain text); spec in cli/SPEC.md § 'Ergonomics: two audiences, one binary'. Parent lesson shows the envelope; this goes inside how it's built and tested.", "color": "#d9dbd3", "nn": "07·1"}, {"slug": "pipelines", "parent": "wbx", "title": "pipelines", "dek": "Build real pipelines with the kernel verbs — query, tangle, and lint accept '-' for stdin, emit JSON, and compose with --json and jq so generated org never touches a temp file.", "evidence": "cli/src/kernel.rs (read_org handles file == \"-\", oql::parse_headlines / tangle_plan / validate all return JSON); the embedded kernel is the same runtime/kernel crate the engine uses; cli/SPEC.md P3 'stdin pipelines across author/trust verbs' names the frontier; parent lesson § agents shows two one-liners but never teaches composition.", "color": "#d9dbd3", "nn": "07·2"}, {"slug": "doctor", "parent": "wbx", "title": "doctor", "dek": "Wire wbx into your shell and let it check itself — doctor's report-not-judge contract (always exit 0, JSON for agents), the bare-wbx where-am-I landing, clap-generated completions, and upgrade's npm-aware self-update.", "evidence": "cli/src/commands.rs:207 (doctor — 'reported not judged', exit 0); cli/src/main.rs (Doctor/Status/Open/Upgrade/Completions verbs, bare wbx routes through doctor at main.rs:258, completions via clap_complete); cli/src/local.rs upgrade(); spec rationale in cli/SPEC.md § 'The prime DX directive'.", "color": "#d9dbd3", "nn": "07·3"}, {"slug": "deploy-kit", "parent": "wbx", "title": "deploy kit", "dek": "Stand up an engine with the deploy-kit — scaffold deployment.org with init, validate, apply, and watch one verb set converge a local container or a cloud provider, with secrets declared by name and never written into the artifact.", "evidence": "cli/src/deploy/mod.rs (DeployVerb init/validate/apply/status/logs/down/local/doctor/secrets; provider-agnostic recipes — local container seam built in, anything else a providers/<place>/bootstrap.sh recipe, fly bundled via include_str!; secrets stored 0600 under the app dir, names in #+DEPLOY_SECRETS, values never in deployment.org); recipe spine at cli/deploy-kit/providers/_recipe.sh; cli/deploy-kit/deployments/*.org templates.", "color": "#d9dbd3", "nn": "07·4"}, {"slug": "the-seam", "parent": "wbx", "title": "the seam", "dek": "Why the same crate compiles to your laptop and into the engine's sandbox — one Io trait (http/read/write/spawn) as the only place the two targets differ, native std/reqwest on one side, Dock-brokered host imports on the other, plus engine discovery via runtime.json.", "evidence": "cli/src/io.rs (trait Io, platform() compile-time impl pick, native module with Disco discovery mirroring Workbooks.Desktop.discovery_path — WB_DESKTOP_DIR / sh.workbooks/disco/runtime.json); cli/SPEC.md § 'The discipline that makes both targets work'; cli/src/dev.rs shows the seam's consequence (dev bails on wasm, agents use bundle); parent lesson § everywhere states the claim — this is the implementation.", "color": "#d9dbd3", "nn": "07·5"}, {"slug": "recipes", "parent": "wbx", "title": "recipes", "dek": "Bring-your-own-cloud without a recompile: a provider is a bootstrap.sh that sources the neutral recipe spine and fills five hooks (ensure_app, set_secrets, attach_volume, deploy_image, public_url) plus the lifecycle trio; recipes resolve from WB_PROVIDERS_DIR, the repo, or copies embedded in the binary — fly is just the bundled one.", "evidence": "cli/deploy-kit/providers/_recipe.sh (hook spine, wb_recipe_run dispatch, WB_ENGINE_ENV assembly); cli/deploy-kit/providers/fly/bootstrap.sh (full hook implementation, prebuilt vs app-build image strategies, synthesized fly.toml); cli/src/deploy/mod.rs:78-80,276-354 (include_str! embedded recipes, resolve_providers_dir chain, env -i hermetic spawn)", "color": "#d9dbd3", "nn": "07·6"}, {"slug": "distribution", "parent": "wbx", "title": "distribution", "dek": "One static binary, three install lanes: a GitHub Release tagged wbx-v* feeds curl installs, an npm launcher whose postinstall downloads the matching platform asset (with PATH-shadow detection and loud failure banners), and wbx upgrade self-swaps the binary in place — plus a wb.wasm target published for in-sandbox agents.", "evidence": "cli/npm/install.js (asset resolution, redirect-following download, findShadow PATH check, non-fatal failure banner); cli/src/local.rs:585-634 (upgrade: release discovery, npm-install detection, atomic binary swap); cli/SPEC.md Distribution section (curl + npm optionalDependencies + wasm lanes); cli/npm/bin/wbx.js", "color": "#d9dbd3", "nn": "07·7"}, {"slug": "validations", "parent": "workflows", "title": "validations", "dek": "DONE is a claim until a check proves it — the three-rung ladder of plan validation (dangling inputs, missing langs), task acceptance (:done-when: and :check blocks run fail-closed in the WASM shell), and build conformance, which together turn drift from a shrug into a named diagnostic.", "evidence": "runtime/kernel/src/lib.rs validate() (dangling :in, component without source block); runtime/host/workflow/todo.ex validate/sh_ok ('unit tests for org mode', fail-closed via Workbooks.Shell, never native sh); runtime/host/conformance.ex (engine-world conformance at wb build); runtime/host/demos/kernel.ex demo_validate. Parent's #honest section promises 'drift detectable, with a name and a line number' — this lesson is that promise, mechanized. User-named topic.", "color": "#f2ddb0", "nn": "08·2"}, {"slug": "schedules", "parent": "workflows", "title": "schedules", "dek": "The full grammar of time in a plan — active vs inactive timestamps, +1w/.+/++ repeaters, the :SCHEDULE: cron property — and the keeper, the engine that actually fires them: workers, crews, lifecycle specs, and the breather between ticks.", "evidence": "runtime/kernel/src/lib.rs parse_timestamp (active/inactive, repeat tokens) + schedule_of (SCHEDULE cron prop wins over SCHEDULED timestamp); runtime/host/keeper.ex (singleton facade, env contract, tick intervals, continuous mode) + runtime/host/keeper/{worker,crew,lifecycle}.ex; runtime/host/workflow.ex surfaces schedule in every run record. Parent's #schedules section says 'the engine fires these' in one sentence — this is the engine. User-suggested topic.", "color": "#f2ddb0", "nn": "08·3"}, {"slug": "boards", "parent": "workflows", "title": "boards", "dek": "Why there is no board model anywhere in the runtime — a board is the kernel's render() over the plan file plus a write path back into it, and any surface (kanban, status page, an agent's ready list) is the same one-file two-way binding.", "evidence": "runtime/kernel/src/lib.rs render() + wit/world.wit (render export); runtime/host/web.ex workbook_page (rendered org + ws/call write path back to the same runtime) and /api/workflow?plan=1 (the plan view with no execution); runtime/host/workflow.ex moduledoc: 'there is no separate board... no DSL, no board model'; _status.json per-task states as a live board in web.ex. Goes deeper than the parent's #boards section by showing the actual render/write seam.", "color": "#f2ddb0", "nn": "08·4"}, {"slug": "waves", "parent": "workflows", "title": "waves", "dek": "How a compiled world executes — topological waves where every step whose predecessors are done runs in parallel, producers' stdout piped into consumers' stdin along the edges, and agent-lang components fanning out sub-agents inside the same DAG.", "evidence": "runtime/host/package_manager.ex run_world/waves (wave computation, Task.async_stream max_concurrency 8, edge piping) and run_component_step (build + run as WASM filter); runtime/host/workflow.ex step/2 (agent-lang component = agent loop, source block as system prompt); runtime/host/web.ex POST /api/workflow. The parent shows three sequential 'rounds'; this shows why independent work actually runs at once.", "color": "#f2ddb0", "nn": "08·5"}, {"slug": "worlds", "parent": "workflows", "title": "worlds", "dek": "What tangle_plan actually computes — edges inferred by matching :out to :in, exports as the outputs nobody consumed, imports from :uses, nested workflows as sub-worlds — and why the result is shaped like a WIT world on purpose.", "evidence": "runtime/kernel/src/lib.rs build_world/build_plan/sig_of/parse_args (:deps/:uses/:in/:out/:persist/:dir header args); runtime/kernel/wit/world.wit (the kernel itself rides the same Component Model it imposes); runtime/host/demos/kernel.ex demo_tangle/demo_nested/demo_persist. The parent's #compile section shows one diagram of this; the deep dive walks the actual inference, including :persist orthogonal persistence and :dir project references it never mentions. User-suggested topic (compile-to-world / components and edges).", "color": "#f2ddb0", "nn": "08·6"}, {"slug": "upgrades", "parent": "workflows", "title": "upgrades", "dek": "How a live plan changes safely — the Motoko/Candid-style subtyping rules (exports may grow but never shrink, imports may shrink but never grow, an output type may never change) that let the kernel refuse a breaking redeploy before it runs.", "evidence": "runtime/kernel/src/lib.rs check_upgrade + world_sigs (the exact rules, error vs warn diagnostics); runtime/kernel/wit/world.wit check-upgrade export; runtime/host/demos/kernel.ex demo upgrade; pairs with :persist in parse_args (state that survives the redeploy the gate just approved). Wholly absent from the parent — pure new depth.", "color": "#f2ddb0", "nn": "08·7"}, {"slug": "dispatch", "parent": "workflows", "title": "dispatch", "dek": "A conversational agent that hands work to background workflows and hears back: an authored org workflow per request, a supervised time-bounded run bound to a task ledger, completions queued as feedback the agent drains on its next turn, and a git-visible TASKS.org rendered from the runtime — never hand-edited.", "evidence": "runtime/host/groundskeeper/tasks.ex (ledger GenServer, run_async supervision + 35-min bound, drain_feedback loop, orphan reset), runtime/host/groundskeeper/board.ex, examples/groundwork/workflows/research-e2b-e2b-dev-and-daytona-daytona-3.org (a persisted Mercury-authored workflow), examples/groundwork/TASKS.org", "color": "#f2ddb0", "nn": "08·8"}, {"slug": "queries", "parent": "vfs", "title": "queries", "dek": "How to actually query the disk: the vfs table's real schema, the Policy-gated vfs-query Dock import your component calls, error-as-JSON safety so a bad query can never crash the engine, and one SQL statement run across a whole library of workbooks.", "evidence": "runtime/host/vfs.ex (CREATE TABLE vfs (volume, path, content, mtime), query_json/2 returns JSON rows or a JSON error — never a host crash); runtime/host/instance/imports.ex (the \"vfs\" cap grants the vfs-query import; gating is by construction); runtime/host/library.ex (Library.query/2 — cross-workbook query-through over every member's vfs.sqlite)", "color": "#a8d4f0", "nn": "09·1"}, {"slug": "volumes", "parent": "vfs", "title": "volumes", "dek": "The disk isn't one undifferentiated tree — it's three named regions (workspace, memory, tmp) with different lifetimes, and knowing which one a byte lives in tells you whether it persists, travels, or evaporates.", "evidence": "runtime/host/vfs.ex (@volumes ~w(workspace memory tmp), the (volume, path) primary key, clear/2); runtime/host/lifecycle.ex (resume/3 clears tmp, workspace+memory persist across freeze/resume); runtime/host/private.ex (@public_volumes — only workspace ships)", "color": "#a8d4f0", "nn": "09·2"}, {"slug": "sync", "parent": "vfs", "title": "sync", "dek": "What 'the disk gets a heartbeat' actually means: the instance lifecycle state machine, freeze/resume as a file copy instead of a VM snapshot, WAL-mode streaming to off-box replicas, and clone-per-tenant so a thousand users each get their own copy of one template disk.", "evidence": "runtime/host/lifecycle.ex (created→active→suspended→frozen→archived transitions, idle auto-demotion thresholds, freeze/resume/clone_for); runtime/host/litestream.ex (enable_wal, replicate to file:// or s3://R2, one-shot restore); parent lesson's #readings section only gestures at this", "color": "#a8d4f0", "nn": "09·3"}, {"slug": "privacy", "parent": "vfs", "title": "privacy", "dek": "Sharing exposes the work, never the session that produced it — one boundary module decides what leaves on every egress path (bundle, git, library), so agent memory, scratch files, and telemetry stay home unless the owner explicitly opts in.", "evidence": "runtime/host/private.ex (the single public/private source of truth: private globs, gitignore_lines, public_volumes); runtime/host/vfs.ex (public_only/1 deletes private volumes + VACUUMs before egress); runtime/host/bundle.ex (ship/4 strips by default, include_private: true is the explicit opt-in, manifest records private_included)", "color": "#a8d4f0", "nn": "09·4"}, {"slug": "sealed-sections", "parent": "vfs", "title": "sealed sections", "dek": "How part of a shipped disk can be ciphertext to everyone and plaintext to the right person: AES-256-GCM envelopes whose content keys never ride in the bundle — they're escrowed at the runtime, released only after a posture check, and optionally wrapped to a recipient's DID so there's no plaintext-key honeypot anywhere.", "evidence": "runtime/host/bundle/sealed.ex (wbseal1 envelope, AAD-bound key_id so entries can't be relabelled, fail-closed open); runtime/host/bundle/escrow.ex (runtime-held keys, release via POST /rcp/key/:key_id only after Workbooks.Access.enforce); runtime/host/bundle/key_wrap.ex (X25519 sealed-box wrapping to a recipient identity); runtime/host/access.ex", "color": "#a8d4f0", "nn": "09·5"}, {"slug": "foreign-tables", "parent": "vfs", "title": "foreign tables", "dek": "Make FROM reach past the file: register a data-source plugin and a query against its entity routes to a live API instead of the local store — or run the sync daemon and keep a resident, queryable, agent-airgapped mirror of the outside world inside the disk.", "evidence": "runtime/host/data_source.ex (the data-source behaviour + entity registry); runtime/host/federation.ex (discover plugins from manifest.org, route SELECT … FROM <entity> to the plugin, else fall through to the local VFS); runtime/host/plugin/sync.ex (interval pull mirroring rows into :task: org nodes in the VFS); runtime/host/data_source/http.ex (the generic HTTP reference source)", "color": "#a8d4f0", "nn": "09·6"}, {"slug": "vectors", "parent": "vfs", "title": "vectors", "dek": "The tenant-scoped semantic index: pure-Elixir Model2Vec embeddings (no model runtime, just vocab + matrix + mean-pool), machine-probed embedding tiers, and one store interface over brute-force SQLite or pgvector ANN.", "evidence": "runtime/host/vector.ex (backend-aware store, cosine vs <=> ), runtime/host/embed/model2vec.ex (static embeddings in :persistent_term), runtime/host/embed/capability.ex (tier probe: big_multimodal/clip/model2vec), runtime/docs/VECTOR-QUERY.org", "color": "#a8d4f0", "nn": "09·7"}, {"slug": "backends", "parent": "vfs", "title": "backends", "dek": "The deploy-kit one-screen config: identical runtime code, swappable storage — blob store as local filesystem or any S3/R2 endpoint, structured data as SQLite or any Postgres URL (which also flips vector search from brute-force to pgvector ANN), with every operation tenant-scoped above the backend so swapping can never widen access.", "evidence": "cli/deploy-kit/storage.env.example (the full knob surface: WB_STORAGE, WB_S3_*, WB_DATABASE_URL, WB_EMBED); runtime/host/storage.ex + runtime/host/storage/local.ex + runtime/host/storage/s3.ex (the adapter seam); runtime/host/db.ex + runtime/host/vector.ex (SQLite↔Postgres + vector backend switch)", "color": "#a8d4f0", "nn": "09·8"}, {"slug": "escrow", "parent": "vfs", "title": "escrow", "dek": "Gated bundle bytes are noise without a key release: content keys are escrowed runtime-side behind posture checks, or sealed-box wrapped to recipient X25519 identities so no plaintext-key honeypot ever exists.", "evidence": "runtime/host/bundle/escrow.ex (runtime-held key store, release over POST /rcp/key/:key_id after Access.enforce), runtime/host/bundle/key_wrap.ex (ephemeral ECDH → KEK → AES-256-GCM wrap), runtime/host/bundle/sealed.ex, runtime/host/did/x25519.ex (did:key Ed25519→X25519 linkage)", "color": "#a8d4f0", "nn": "09·9"}]

const REVIEW_SCHEMA = {
  type: 'object',
  required: ['verdict', 'findings'],
  properties: {
    verdict: { type: 'string', enum: ['pass', 'fail'] },
    findings: { type: 'array', items: { type: 'string' } },
  },
}

const REPO = '/Users/shinyobjectz/Apps/workbooks'

const lessons = LESSONS

const results = await pipeline(
  lessons,

  // ── 1 · RESEARCH: a facts dossier nobody can hand-wave past ──
  (l) => agent(
    `Research dossier for the deep-dive lesson "${l.title}" (slug ${l.slug}, under the "${l.parent}" lesson) on workbooks.sh/learn. Repo: ${REPO}.

Dek: ${l.dek}
Evidence trail (verify and EXPAND it — read these files plus whatever they lead to): ${l.evidence}

Also read ${REPO}/web/learn/${l.parent}.html fully — the parent's scope, voice, and what it already teaches. The deep dive must go DEEPER, never repeat.

Produce a dossier as your final message:
1. FACTS — every claim the lesson can make, each with the file:line or module that proves it. Include real constants, real shapes, real flows (e.g. actual env vars, actual function names, actual limits). Mark anything uncertain as UNCERTAIN rather than guessing.
2. NARRATIVE ARC — the problem the reader has, the aha, the mechanics, the honest limits. What would a USER actually want from this page?
3. SECTION OUTLINE — 8-12 sections in house order (problem → definition → mechanics/depth sections → honesty → FAQ), with one line each on what it covers and which diagram (mermaid flowchart/sequence/table) would genuinely explain it. Mark depth-rung (skippable) sections.
4. CROSS-LINKS — which other lessons/deep-dives this should reference (extensionless hrefs).
5. THREE WORKED DETAILS — concrete examples/snippets (real org, real commands, real output shapes) the writer must include so nothing stays abstract.`,
    { label: `research:${l.slug}`, phase: 'Research' }
  ).then((dossier) => ({ l, dossier })),

  // ── 2 · WRITE: the full page + narration script ──
  ({ l, dossier }) => agent(
    `Write the COMPLETE deep-dive lesson "${l.title}" for workbooks.sh/learn. Repo: ${REPO}. You are writing FILES, not a reply.

THE DOSSIER (your single source of facts — do not invent beyond it):
${dossier}

FORMAT CONTRACT — study these exemplars first and match them exactly:
- ${REPO}/web/learn/workflows.html (structure, mermaid usage, voice)
- ${REPO}/web/learn/vfs.html (depth benchmark)
- ${REPO}/web/learn/autopoet.html (a sub-lesson: kicker style "learn / NN·i — under <parent> · <title>")

Write ${REPO}/web/learn/${l.slug}.html:
- html style: --pc:${l.color}; --ac:#13d943. Kicker: "learn / ${l.nn} — <b>under ${l.parent} · ${l.title}</b>". Three-line h1 with ONE Groothan .bub word (the formula: the bub word fills with the page color automatically).
- Hero figure: <img src="img/${l.slug}-hero.jpg"> with a descriptive 1970s-sci-fi alt (the art is generated separately; write alt text that doubles as the art brief — subject matching the lesson's metaphor, bright, monumental-vs-small-figure).
- Full TOC, 8-12 sections per the dossier outline, defcard with the canon-noun definition, 2-4 brand mermaid diagrams (<pre class="mermaid">, include <script type="module" src="learn-mermaid.js"></script>), pre.orgsample for real snippets, .cmp tables where comparing, an HONESTY section, FAQ (4-6 real questions), lnext cards (parent lesson first), workbook-spec org block (SLUG ${l.slug}, NN ${l.nn}, PC ${l.color}), window.LEARN mix led by ${l.color}, nav include <script src="../nav.js?v=8"></script>, learn.css?v=4, audio.js defer.
- VOICE CANON (hard rules): agents are "agents" never "crew"; never "software that runs itself"; honesty over hype; em-dashes; short declaratives; ALL internal links extensionless; an HTML-comment easter egg after <body> that teaches this lesson's idea to source-readers.
- DEPTH: every section must say something the dossier proves. No filler, no "simply", no hand-waving. If the dossier marked something UNCERTAIN, either omit it or state the uncertainty honestly.

Write ${REPO}/web/learn/audio/scripts/${l.slug}.json:
- Read ${REPO}/web/learn/audio/README.md first — its rules are HARD (no double-quote chars in text, only the proven tag set, 10-16 tags, ellipses beats, every list enumerated First…Second…, never read a table aloud).
- Same structure as scripts/autopoet.json: slug, title "${l.title} — under ${l.parent}", tracks matching the page sections (intro…outro), intro says "Lesson ${l.nn.replace('·', ' point ')}" and names the parent; outro hands back to the parent lesson.
- EYES-CLOSED RULE (README rule 6, hard): every track opens by speaking its section's headline naturally; every mermaid diagram, org sample, and table in that section gets a spoken walkthrough woven into the narration (describe the graph as a story, read the sample aloud, give the table's verdict) — never "as shown above/below". A driver must fully follow the argument.

VERIFY before finishing: the JSON parses (python3 json.load); zero '\"' inside script text values; zero internal .html hrefs in the page; mermaid loader present if any pre.mermaid; all includes present. Return one line per file written plus the section count.`,
    { label: `write:${l.slug}`, phase: 'Write', model: 'opus' }
  ).then((note) => ({ l, dossier, note })),

  // ── 3 · REVIEW: adversarial gate ──
  ({ l, dossier, note }) => agent(
    `Adversarially review the new lesson ${REPO}/web/learn/${l.slug}.html and ${REPO}/web/learn/audio/scripts/${l.slug}.json. Try to FAIL it.

Check, with evidence:
1. FACTS: sample 6+ technical claims from the page and verify each against the actual code (the dossier cited: ${l.evidence}). Any claim the code contradicts = fail.
2. DEPTH: would a reader who needs "${l.title}" leave satisfied? Flag hand-waving, filler sections, or anything the parent lesson (${REPO}/web/learn/${l.parent}.html) already covers verbatim.
3. CANON: "crew" used for agents = fail. "runs itself" framing = fail. Missing honesty section = fail. Internal .html hrefs = fail. Bub word count != 1 in h1 = fail.
4. MECHANICS: audio JSON parses + obeys README rules (no double quotes in texts, tags from the proven set only, lists enumerated); page includes learn-mermaid.js iff pre.mermaid exists; workbook-spec block present; mermaid blocks have plausible syntax (no obvious parse-breakers like unquoted special chars in node labels).
5. LINKS: every internal href resolves to an existing page in ${REPO}/web/learn/ (extensionless → file.html) or is an anchor.

If anything fails: FIX IT YOURSELF in the files (you have write access) when the fix is mechanical (links, quotes, canon words, missing includes), and re-check. Only verdict=fail for problems requiring a rewrite (factual contradictions, hollow depth). findings = everything you found and what you did about it.

Writer's note for context: ${note}
Dossier summary (first 2000 chars): ${String(dossier).slice(0, 2000)}`,
    { label: `review:${l.slug}`, phase: 'Review', schema: REVIEW_SCHEMA, model: 'opus' }
  ).then((review) => ({ slug: l.slug, parent: l.parent, nn: l.nn, verdict: review.verdict, findings: review.findings }))
)

return results.filter(Boolean)
