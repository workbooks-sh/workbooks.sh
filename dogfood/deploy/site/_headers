# Cloudflare Pages headers — fonts are content-addressed by filename in
# practice (new face = new file), so cache them hard.
/fonts/*
  Cache-Control: public, max-age=31536000, immutable
/learn/img/*
  Cache-Control: public, max-age=604800
