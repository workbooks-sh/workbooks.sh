---
name: workbook_concepts
description: The rationale behind workbooks: literate programming, WebAssembly as render target, and docs-as-a-workbook. Use when explaining why the model works the way it does or making a design choice that should align with it.
---

# Workbook concepts

<!-- GENERATED by `work weave` from skills/workbook_concepts.work — do not edit; edit the source docs it composes. -->

The why behind the model: literate programming as the authoring paradigm, WebAssembly
as the universal render target, and why we author even our own docs as a workbook.
This skill grounds an agent in the rationale so its choices align with the model
rather than fight it.

When the user asks "why is it built this way" or needs the philosophy behind a
decision, load the references below.

## References

Load a reference below only when you need that depth.

### Foundations

- [Literate Programming](references/literate-programming.work) — Most code is written for the machine first and the human second.
- [Why WebAssembly](references/why-webassembly.work) — A literate document that mixes languages needs a common ground to run the

### In practice

- [Why we author these docs as a workbook](references/why-we-author-docs-as-a-workbook.work) — > A short post on dogfooding — the docs you're reading are built with the thing they document.
