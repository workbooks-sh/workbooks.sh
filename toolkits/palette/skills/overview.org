#+TITLE: palette — overview
#+TOOLKIT: palette
#+SKILL: overview
#+TAGS: wasm runtimes compilers sandbox build javascript python ruby
#+STATUS: experimental

* When to use this
  :PROPERTIES:
  :NETWORK: no
  :DESTRUCTIVE: no
  :END:

  The palette is the one set of WASM build/runtime tooling. Each runtime is a pinned
  prebuilt (or build recipe) that runs untrusted source in the capability-gated
  sandbox — no OS process, no host access beyond a granted preopen.

* Build the runtimes (from the committed pinned specs)

  #+CAPTION: build + register every runtime in the set
  #+begin_src bash
  work toolkit build palette
  #+end_src

  #+CAPTION: or just one
  #+begin_src bash
  work toolkit build palette python
  #+end_src

* Run source in the sandbox

  #+begin_src bash
  qjs    /w/app.js
  python /w/app.py
  ruby   /w/app.rb
  #+end_src

  Each runtime's own resources (stdlib) preopen automatically; grant the script's
  dir as an extra preopen.
