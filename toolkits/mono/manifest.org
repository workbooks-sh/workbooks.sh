#+TITLE: mono toolkit
#+TOOLKIT: mono
#+VERSION: 0.1.0
#+STATUS: exp
#+TAGLINE: RGBA → grayscale, the first deployed kernel-shape toolkit (bytes→bytes hot loop).
#+EXEC: kernel
#+BUILD_LANG: c
#+BUILD_SRC: path:src
#+CLI_BIN: mono
#+CAPS:

* mono — grayscale kernel                                       :toolkit:
  :PROPERTIES:
  :ID:        mono
  :VERSION:        0.1.0
  :CLI_BIN:   mono
  :STATUS:    exp
  :SKILL_DIR: skills/
  :END:

  The DEBUT of the =#+EXEC: kernel= shape: a =bytes → bytes= reactor compiled
  from C (in-sandbox, clang.wasm) and looped by the fabric — instantiate ONCE,
  call per frame. Input: RGBA pixels; output: same-length RGBA with each pixel
  replaced by its luminance (integer average). Trailing non-quad bytes pass
  through unchanged.

  : #+EXEC: kernel            opened once by Workbooks.Kernel, arena: :exports
  : #+BUILD_SRC: path:src     C source → Compilers.c_compile_to_kernel
  : process(in_len)→out_len   the kernel ABI; in_ptr()/out_ptr() locate the arena

  Build with =wb toolkit build mono= → KernelRegistry registers =mono= → loop it
  with =Workbooks.Fabric.map_kernel= (engine) or one-shot via =POST /rcp/kernel/run=.

  Skill index (read on demand):

  | Skill      | Use when                                   |
  |------------+--------------------------------------------|
  | =overview= | First contact — what this kernel does + ABI |
