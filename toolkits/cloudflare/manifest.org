#+TITLE: cloudflare toolkit
#+TOOLKIT: cloudflare
#+VERSION: 0.1.0
#+CLI_BIN: wrangler
#+CLI_VERSION_RANGE: >=3.0
#+STATUS: experimental
#+TAGLINE: All-Cloudflare backend for a forged workbook — D1 + Workers AI + Pages, on the user's own account.
#+REQUIRES: wrangler>=3.0
#+SKILL_DIR: skills/

* cloudflare — D1 + Workers AI + Pages, one account                :toolkit:
  :PROPERTIES:
  :ID:        cloudflare
  :VERSION:        0.1.0
  :CLI_BIN:   wrangler
  :STATUS:    experimental
  :SKILL_DIR: skills/
  :END:

  The all-Cloudflare path for a forged workbook: a live, multi-tenant app
  standing entirely on the user's OWN Cloudflare account, with just a
  =wrangler= login — no second provider, no DB to provision elsewhere.

  STATUS: the turnkey one-command deploy (=wb forge app deploy --backend cf-d1=)
  is NOT currently available — the =wb forge= verb and the unified gateway Worker
  (=deploy-kit/app/gateway=) were removed 2026-06-09 (restore-or-drop tracked in
  =wb-dtd0.1=). The skill gives the real raw-=wrangler= primitives to build it
  today; do NOT invoke =wb forge app=.

  - DATA + AI: a Cloudflare Worker over D1 (SQLite, tenant-scoped data) + Workers
    AI. Built with =wrangler d1 create= / =wrangler d1 execute= / =wrangler deploy=.
  - AUTH: BetterAuth-on-D1 mounted in the Worker at =/api/auth/*= when
    =BETTER_AUTH_SECRET= is set (sessions, organizations, JWKS, bearer).
  - HOSTING: the workbook =.html= frontend ships to Cloudflare Pages via the
    =wrangler= toolkit (=wrangler pages deploy=) — that path works today.

  Account selection is automatic: with one account on the login it's derived
  from =wrangler whoami=; with several, set =CLOUDFLARE_ACCOUNT_ID=. The token
  is wrangler's own (=~/.wrangler=) — Workbooks never reads, stores, or proxies
  it. This is the sibling of the =byod= (Postgres + Railway) toolkit; pick
  =cloudflare= when the user wants a single account + bill.

  =wrangler --help=, =wrangler d1 --help=, =wrangler deploy --help= remain
  authoritative for per-flag detail. The skills cover the end-to-end tasks.

  Skill index (read on demand):

  | Skill                   | Use when                                           |
  |-------------------------+----------------------------------------------------|
  | =all-cloudflare-backend= | Stand a forged workbook entirely on the user's Cloudflare account — D1 data + Workers AI + BetterAuth + Pages hosting, one login |
