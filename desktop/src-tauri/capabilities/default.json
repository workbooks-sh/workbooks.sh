{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability — core + sidecar_status + workspace_* + fs_tree (tree_walk/tab_open/open_in_os) + event listening (wb-i38o.2/3/7).",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:default",
    "dialog:allow-open",
    "dialog:allow-confirm",
    "dialog:allow-message",
    "dialog:allow-ask",
    "clipboard-manager:allow-write-text"
  ]
}

