# Resources are (re)generated by `bun run dev` / `beforeDevCommand`.
# If the watcher sees them land while cargo is mid-compile it triggers
# a restart loop, so they must be excluded from the dev watcher.
resources/
binaries/
target/
