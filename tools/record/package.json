{
  "name": "@workbooks/record",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Driver + proof-loop recording harness for the Workbooks Browser (MCP app tier + Playwright web tier) with a Gemini->MiniMax verify cascade.",
  "bin": { "wb-record": "./record.mjs" },
  "scripts": {
    "record": "node record.mjs",
    "postinstall": "playwright install chromium"
  },
  "dependencies": {
    "playwright": "^1.49.0"
  }
}
