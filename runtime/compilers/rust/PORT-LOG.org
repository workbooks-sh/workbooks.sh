#+TITLE: Rust-in-WASM port log (wb-zyl.7)
#+NOTE: append-only running log so the grind loop never loses the thread.

* Goal
  Untrusted Rust compiled AND run ENTIRELY in the wasm sandbox (zero native exec).
  Path 1: mrustc.wasm (Rust→C) → clang.wasm (C→wasm) → run. Path 2: fork rustc→wasm32-wasi.

* Assets already in-repo (reuse, don't rebuild)
  - compilers/clang/ — YoWASP clang/lld in wasm (C→wasm), WORKING end-to-end.
  - Workbooks.Compilers.compile_c/3 + compile_and_run_c/3 (the C→wasm→run lane).
  - The Zig chain (zig_compile_and_run) is the template: producer→C, then clang lane,
    plus bridges (wasi_shim.c, builtin no-op prelude, crt:false).

* 2026-06-07 — Path 1 first-wall probe (zig cc -target wasm32-wasi via kassane/mrustc-zig)
  mrustc now bootstraps Rust 1.90 (modern). `zig build -Dtarget=wasm32-wasi` on mrustc-zig
  compiles INTO the code, then fails in tools/common with ~25 errors:
  - jobserver.cpp: `pipe()` undeclared → WASI has no pipe/fork. FIX: stub jobserver to
    serial (no parallelism) under __wasi__.
  - toml.h/.cpp: `std::ifstream` incomplete + `operator<<(ostream, const char*)` not found
    → zig's WASI libc++ gates <fstream>/<ostream>/localization. FIX options: (a) build with
    wasi-sdk's fuller libc++ instead of zig's; (b) replace std::ifstream with fopen/fread.
  NEXT (in priority): (1) get a wasi-sdk (or YoWASP clang sysroot, which HAS full libc++)
  cross-compile of mrustc instead of zig's libcxx; (2) stub jobserver→serial; (3) build
  past common/ into the compiler core; (4) map deeper walls: 32-bit ptr assumptions,
  4GB memory ceiling (→ memory64), mrustc's subprocess call to the C compiler (intercept
  like clang compile/link split).

* TODO scaffolding
  - compilers/rust/ should hold: pinned mrustc source spec + build recipe (build.sh →
    mrustc.wasm) + the jobserver/io stubs + manifest.org (KIND=compile-to-c-then-wasm).
  - Wire Workbooks.Compilers.rust_compile_and_run/4 (mrustc.wasm .rs→C, then compile_c).

* 2026-06-07 (iter 2) — Path 1 toolchain VALIDATED + recipe encoded
  Switched the cross-compiler from zig's libc++ to *wasi-sdk-33* (its libc++ ships full
  <fstream> + an exceptions-enabled sysroot variant) — this clears the fstream/localization
  wall outright. Patched jobserver.cpp → serial under __wasi__ (no pipe/fork).
  RESULT: tools/common/*.cpp all compile clean to wasm32-wasip1 (the lib that failed under
  zig). Committed the recipe: compilers/rust/{manifest.org, build.sh}:
    - fetch wasi-sdk-33 (sha-pinned, arm64-macos + x86_64-linux) + mrustc @ be69c747
    - patch jobserver (serial) + codegen_c.cpp (under __wasi__: emit C, skip the system()
      C-compile — the clang lane links it separately, exactly like the Zig chain)
    - compile all ~129 src/ + tools/common cpp → link mrustc.wasm
  NEXT (grind): run build.sh end-to-end — expect walls in src/ core: possible 32-bit
  pointer assumptions, missing syscalls/_WASI_EMULATED_*, memory (escalate -W memory64=y +
  --target wasm64 if the compile exceeds 4GB), -fno-rtti viability (mrustc may need rtti →
  drop the flag). Once mrustc.wasm links: register it, run mrustc.wasm on a hello.rs to emit
  C in-sandbox, then Compilers.compile_c that C → wasm → run; wire rust_compile_and_run/4.

* 2026-06-07 (iter 3) — MILESTONE: mrustc.wasm BUILDS + RUNS in the sandbox
  The Rust compiler now executes inside wasm. Full wall sequence solved (all in build.sh):
  1. includes: -Isrc/include (target_version.hpp lives there).
  2. jobserver.cpp pipe()/fork → serial stub under __wasi__.
  3. codegen_c.cpp system() (invokes the C compiler) → skip under __wasi__ (emit C; the
     clang lane links it separately — same split as clang's compile/link).
  4. proc_macro.cpp <spawn.h>/<sys/wait.h> → guarded out under __wasi__ (proc-macros need a
     subprocess; unsupported in-sandbox by design). wasi/wasi_compat.h provides ENOSYS
     stubs for pipe/posix_spawn/waitpid/kill so it COMPILES (errors only if a proc-macro is
     actually used). wasi/sys_stub.cpp defines system()→-1.
  5. zlib (HIR compression, mandatory, no disable) → vendored, compiled to wasm.
  6. version.cpp VERSION_GIT_* → supplied via a generated header.
  7. link: -lwasi-emulated-process-clocks (clock) + -lunwind (_Unwind_CallPersonality,
     __wasm_lpad_context) + EH multilib (auto-selected by -fwasm-exceptions).
  8. THE EH ABI: wasi-sdk-33 emits LEGACY try/catch which wasmtime 45 rejects
     ("legacy_exceptions feature required"). Fix: -mllvm -wasm-use-legacy-eh=false → new
     exnref EH, which wasmtime accepts with -W exceptions=y. (No legacy flag in wasmtime 45.)
  RESULT: `wasmtime run -W exceptions=y mrustc.wasm --version` →
    "rustc 1.29.100 (mrustc v0.12.0 wasm:be69c747)". It also parses a real .rs and loads the
    target (--target i586-linux-gnu; 32-bit, matches wasm32). 16.5 MB module.

  NEXT (the remaining grind to end-to-end):
  - mrustc needs the Rust STDLIB: `Unable to locate crate 'std'`. mrustc's flow compiles the
    stdlib from rustc's source library/ (for MRUSTC_TARGET_VER, e.g. 1.54/1.74) into .hir,
    then user crates link against it. So: fetch rustc-<ver>-src, run mrustc.wasm to build
    libcore/liballoc/libstd IN the sandbox (heavy — watch the wasm32 4GB ceiling → may need
    -W memory64=y + a memory64 build), then compile t.rs → C, then clang lane → wasm → run.
  - Start with the smallest stdlib slice that compiles (core-only / no_std) to prove the
    .rs→C→wasm→run chain, then widen to full std.
  - Wire Workbooks.Compilers.rust_compile_and_run/4 once a .rs runs.

* 2026-06-07 (iter 4) — TESTED the stdlib compile + the "memory thing"
  Fed rustc-1.54.0-src/library/core to mrustc.wasm in-sandbox. Findings:
  - Parse needs `--edition 2018` (core 1.54 uses 2018 macro syntax; default edition errored
    on TOK_FATARROW in core/src/macros/mod.rs).
  - With edition set, it crashes in early compile: "memory fault at wasm address
    0x10000042c in linear memory of size 0x570000". KEY: that address is ~4.0GB but the
    wasm memory is only ~5.7MB and peak RSS is ~253MB. So this is NOT the 4GB ceiling /
    OOM — it's a 32-BIT POINTER BUG: mrustc (only tested on 64-bit hosts) computes/stores
    a pointer as 64-bit somewhere; on wasm32 it lands at a bogus >4GB address.
  - CONCLUSION: the feared wasm32 4GB memory ceiling is NOT the Path-1 wall (mrustc is
    lean). The real wall is mrustc's 64-bit-pointer assumptions. These are findable+fixable
    (grep for uintptr/uint64 pointer packing, tagged pointers, hash-of-pointer, size_t vs
    u64) but tedious — likely several sites.
  NEXT: build a DEBUG mrustc.wasm (with assertions + -g, WASMTIME_BACKTRACE_DETAILS=1) to
  get the exact function at the fault; fix the pointer assumption(s); re-test core. Then
  proper stdlib orchestration (edition/cfg/--extern) ideally via minicargo (build
  minicargo.wasm too) rather than hand-rolled invocations. Memory headroom is fine; if a
  LATER full-std compile ever approaches 4GB, escalate to memory64 — but not needed yet.
  STATUS: Zig fully works end-to-end (re-demoed live: "ZIG IN WASM SANDBOX: 42"). Rust =
  compiler runs+parses in-sandbox; stdlib compile blocked on mrustc 64-bit-ptr bugs.

* 2026-06-07 (iter 5) — BREAKTHROUGH: mrustc.wasm COMPILES libcore fully in-sandbox
  The iter-4 "memory fault at ~4GB" was a STACK OVERFLOW, not a pointer bug and not the 4GB
  heap ceiling: the fault was deep in the recursive-descent parser (Parse_PathNodes ← a long
  Parse_Expr1_2…13 chain), and wasi-sdk's default wasm stack is tiny (64KB). FIX: link with
  -Wl,-z,stack-size=67108864 (64MB). Added to build.sh.
  RESULT: `mrustc.wasm library/core/src/lib.rs --crate-type lib --edition 2018
  --target i586-linux-gnu` runs the FULL pipeline in-sandbox: Parse → Resolve → Monomorph →
  MIR Optimise → HIR Serialise → Trans Codegen DONE. Outputs: libcore.hir.c (21MB of C) +
  libcore.hir.hir (1.36MB metadata). PEAK RSS ~592MB / footprint ~502MB — the wasm32 4GB
  ceiling is comfortably clear. Real Rust (the actual stdlib core crate) compiles in wasm.
  (mrustc's final "Running gcc" to compile its C is our system() no-op stub — by design; the
  clang lane compiles the emitted C.)
  NEXT (the last mile to end-to-end): clang.wasm compiles libcore.hir.c → core objects, then
  compile a small #![no_std] program against libcore.hir → C → clang.wasm → link → run.
  Then widen to full std (alloc+std; bigger C, watch memory but core was only ~600MB) and
  wire Workbooks.Compilers.rust_compile_and_run/4. Edition must be passed (--edition 2018);
  proper multi-crate orchestration ideally via minicargo.wasm later.

* 2026-06-07 (iter 6) — ★ END-TO-END: untrusted Rust compiled AND run in the sandbox ★
  PROVEN: `RUST IN WASM SANDBOX: 55` (fib(10)) — zero native execution. Full chain:
    1. mrustc.wasm: rustc-1.54 library/core → libcore.rlib.{hir,c}  (in sandbox)
    2. mrustc.wasm: prog.rs (no_std, exports rust_compute) → libprog.rlib.c  (in sandbox)
    3. clang.wasm: core.c + prog.c + harness.c → .o  (in sandbox)
    4. wasm-ld: link + crt1-command.o → prog.wasm  (in sandbox)
    5. wasmtime: prog.wasm → "RUST IN WASM SANDBOX: 55"
  The exact working invocation is in e2e.sh (committed). Key settings discovered:
  - mrustc target = arm-linux-gnu (32-bit + i64-align-8 MATCHES wasm32; i586 mismatched →
    SIMD alignment static-asserts failed).
  - --edition 2018 (core 1.54 macro syntax); MRUSTC_TARGET_VER=1.54.
  - mrustc emits ARM inline asm (mcr p15 memory barriers) for the arm target → stubbed to
    no-ops in the emitted C (safe: single-threaded wasm). [The clean fix is a proper wasm32
    mrustc target spec with no asm — a follow-up; the stub is honest + correct for now.]
  - clang flags for mrustc's C: -fno-strict-aliasing -fwasm-exceptions -mllvm
    -wasm-enable-sjlj (mrustc uses setjmp/longjmp) -mllvm -wasm-use-legacy-eh=false.
  - link: crt1-command.o + objects + -lc + libclang_rt.builtins.a (no -lunwind needed).
  - mrustc.wasm MUST be linked with -Wl,-z,stack-size=67108864 (recursive parser).
  Peak memory across the whole thing stayed ~600MB — the wasm32 4GB ceiling never bit.

  SCOPE/honesty: this is real Rust (the actual core crate + an untrusted user crate), no_std,
  edition-pinned (mrustc ~1.54 mode), no proc-macros. REMAINING (enhancements, not blockers):
  full std (alloc+std → bigger C, likely still <4GB), a proper wasm32 mrustc target spec
  (drop the asm stub), and wiring Workbooks.Compilers.rust_compile_and_run/4 + a libcore
  prebuild into the runtime so it's a first-class lane like C/Zig. The PHYSICAL question is
  answered: Rust-in-sandbox is POSSIBLE and DONE.

* 2026-06-07 (iter 7) — FULL STD plan (confirmed achievable from source)
  GOAL: a real `fn main(){ println!("hello") }` compiled + run in-sandbox (not just no_std).
  WHY core worked but std needs more: std needs an OS backend; mrustc only had unix targets,
  so std would pick its UNIX backend → POSIX deps (pthread/socket/fork) wasi lacks.
  THE FIX (verified in source):
  - std/src/sys/mod.rs selects the backend on `cfg(target_os = "wasi")` (checked first). So
    we just need mrustc's target to say os=wasi.
  - mrustc loads CUSTOM target-spec FILES (src/trans/target.cpp load_spec_from_file): keys
    target.family / os-name / env-name / arch. arch must be a built-in (arm/x86/...).
  - ARCH_ARM32 = {ptr 32, align i16=2,i32=4,i64=8,i128=16,f32=4,f64=8} = EXACTLY wasm32's
    ABI (why arm worked for core). So spec = { family="", os-name="wasi", arch="arm" } makes
    std compile its WASI backend with wasm32-correct layout.
  - Optional clean finish: add ARCH_WASM32 (~6 lines in target.cpp) so target_arch="wasm32"
    → drops core's ARM-barrier asm stub entirely. Not required to get std working.
  PLAN:
  1. Build minicargo.wasm (mrustc's tools/minicargo, same C++/wasi recipe as mrustc) — std is
     a CRATE GRAPH (core→alloc→std + libc/cfg-if/hashbrown/std_detect/unwind/panic_*), needs
     orchestration; hand-rolling per-crate -L is error-prone.
  2. Write compilers/rust/wasm32-wasi.spec (the target file above). Commit it.
  3. minicargo.wasm builds libstd from rustc-1.54 library/ with that target, in-sandbox.
  4. Compile a hello.rs (uses std/println) → C → clang.wasm (+ wasi shim, sjlj, stack) →
     link → run → "hello". Watch memory (core was ~600MB; std bigger but likely <4GB).
  5. Wire Workbooks.Compilers.rust_compile_and_run/4 once hello works.
  EXPECT: more mrustc bugs in std (std ≫ core), more bare-wasi symbols (extend wasi_shim.c),
  edition/cfg per-crate (minicargo + the rustc-1.54 overrides toml handle this).

* 2026-06-07 (iter 8) — wasm32-wasi target spec WRITTEN + validated
  Dumped arm-linux-gnu via mrustc --dump-target-spec, edited → wasm32-wasi.spec:
  family="", os-name="wasi", env-name="", arch=arm (wasm32 ABI), no -pthread.
  VALIDATED: mrustc.wasm --target /path/wasm32-wasi.spec compiles libcore (os=wasi loads +
  Codegen DONE). This is the enabler — with os=wasi, std will select sys/wasi (not unix).
  Committed compilers/rust/wasm32-wasi.spec.
  NEXT: build minicargo.wasm (same recipe as mrustc), then minicargo builds core→alloc→std
  with this spec, then a println hello.rs → C → clang.wasm → run.

* 2026-06-07 (iter 9) — ARCHITECTURE PIVOT: minicargo.wasm is a DEAD END; host orchestrates
  minicargo's entire job is posix_spawn(mrustc) per crate (tools/minicargo/jobs.cpp:255,
  os.cpp). No subprocess in WASI → minicargo CANNOT run in-sandbox. Corrected design:
  - The ORCHESTRATOR is the HOST (our runtime), which runs mrustc.wasm once per crate in dep
    order — exactly as we already split clang's compile/link into host-driven steps.
  - The std crate-build PLAN (crate order + per-crate flags: --crate-name/-type, -L, --extern,
    --cfg, edition) is STATIC. Capture it ONCE from NATIVE minicargo (build native mrustc +
    minicargo, run it on rustc-1.54 library with --target wasm32-wasi.spec, verbose/dry-run to
    dump each mrustc command), then commit that command sequence as a replay script. The host
    replays each as an mrustc.wasm invocation in-sandbox → libstd.hir/.c.
  - SECURITY note: libstd is TRUSTED (the standard library), so prebuilding it is fine; the
    UNTRUSTED user .rs always compiles in-sandbox (mrustc.wasm) against the prebuilt libstd.
    Host-replay keeps even the std build in-sandbox for purity, but it's not a security req.
  NEXT: native mrustc+minicargo build (in progress) → capture the std plan → replay core→
  alloc→std with mrustc.wasm + wasm32-wasi.spec → compile hello.rs (println) in-sandbox →
  clang.wasm + libstd.c → run "hello".

* 2026-06-07 (iter 10) — native mrustc+minicargo built; the exact libstd build path
  Built native mrustc (27MB) + minicargo (1.2MB) from the pinned source. libstd is NOT a
  point-minicargo-at-library/std job — minicargo.mk:256 shows it needs mrustc's
  version-specific scaffolding: source PATCH (rustc-1.54.0-src.patch), --script-overrides
  (build-script overrides dir), --manifest-overrides rustc-1.54.0-overrides.toml, --vendor-dir.
  The whole thing is orchestrated by `make -f minicargo.mk`.
  CONCRETE NEXT (the build): from the mrustc source tree —
    make -f minicargo.mk RUSTC_VERSION=1.54.0 MRUSTC_TARGET=<abs path to wasm32-wasi.spec> \
      output-1.54.0-<suffix>/libstd.rlib
  This downloads+patches rustc-1.54 src, sets up overrides, and builds core→alloc→std (+deps)
  with NATIVE mrustc for our wasi target → libstd.{hir,c}. std is TRUSTED, so this native
  prebuild is acceptable (like building mrustc.wasm itself natively); the UNTRUSTED user .rs
  then compiles IN-SANDBOX (mrustc.wasm) against these libstd.hir.
  Expect walls: MRUSTC_TARGET as a spec-file path may tangle with the makefile's RUSTC_ARCH
  derivation (it splits RUSTC_TARGET on '-'); may need to set RUSTC_ARCH=arm explicitly, or
  add wasm32-wasi as a recognized name. The std-1.54 wasi backend may surface mrustc bugs.
  AFTER libstd exists: (a) clang.wasm compiles each libstd .c → objects; (b) mrustc.wasm
  compiles hello.rs (println) in-sandbox against libstd.hir → hello.c; (c) clang.wasm hello.c
  + libstd objs + wasi_shim + crt1 → wasm-ld → run → "hello".
  PURITY upgrade (later): replay the per-crate mrustc commands (minicargo -v dumps them) with
  mrustc.wasm so even libstd builds in-sandbox.

* 2026-06-07 (iter 11) — libstd build wired up; current wall = layout asserts under arch=wasm32
  Progress (all reproducible, committed under compilers/rust/std/):
  - native mrustc+minicargo build the std graph via `make -f minicargo.mk RUSTC_VERSION=1.54.0
    MRUSTC_TARGET=<wasm32-wasi.spec> OUTDIR=output-wasi/ LIBS` (MRUSTC_TARGET must be a PATH;
    MRUSTC_TARGET_VER=1.54 — NOT 1.54.0).
  - The source scaffolding works (patch/vendor/overrides). Added missing wasi override
    build_dlmalloc.txt (empty; dlmalloc build.rs emits nothing) — wasi pulls dlmalloc.
  - Added ARCH_WASM32 to mrustc (mrustc-arch-wasm32.patch) so target_arch=wasm32.
  - cc step: mrustc invokes `wasm32-wasi-gcc`; wrapper → wasi-sdk clang cross-compiles the
    trusted libstd C → wasm objects (cc-wrapper.template.sh).
  - libcore.{c,hir} ARE produced for wasi.
  CURRENT WALL: with arch=wasm32, the cc step fails on size/align static-asserts in
  libcore.rlib.c (e.g. OptionSome sizeof==24/alignof==8) — mrustc's computed layout != what
  wasm32 clang computes for some types. NOTE: arch=arm did NOT have this (the no_std e2e
  proved arm's layout matches wasm32 clang exactly). So either ARCH_WASM32's params are
  slightly off (atomics? I set all-true vs arm's u16/u64=false — may shift Option<Atomic>
  layouts), OR target_arch=wasm32 pulls core code whose layout mrustc miscomputes.
  NEXT (two options): (A) PRAGMATIC — use arch=arm for the std build too (proven layout) and
  have the cc-wrapper sed-stub the 3 ARM barrier asms before compiling (reuses the working
  core path). (B) CLEAN — fix ARCH_WASM32 params to match wasm32 clang exactly (diff
  mrustc's computed sizes vs clang's; likely the atomics flags). Try (A) first — it's known
  to work for core. Then: build full std, compile a println hello.rs in-sandbox against it,
  link, run. Memory headroom fine.

* 2026-06-07 (iter 12) — std wall = mrustc layout discrepancy under os=wasi (deep, not a flag)
  Tried both arch=wasm32 AND arch=arm for the std build: BOTH fail the SAME way — libcore.rlib.c
  size/align static-asserts fail at the cc step (e.g. OptionSome: mrustc says sizeof==24, clang
  computes otherwise). This is NOT arch and NOT the asm-stub (applied): it's that minicargo
  builds core with os-name="wasi" cfg + `--cfg debug_assertions`, and under THAT cfg combo
  mrustc's own layout computation disagrees with clang for some Option/struct instantiations.
  The no_std e2e did NOT hit this because it built core with the builtin arm-linux-gnu target
  (os=linux) + no debug_assertions — different cfg path, different (correct) layouts.
  => Genuine mrustc bug territory (layout under the wasi cfg), needs real debugging:
     - inspect the emitted struct s_...OptionSome in libcore.rlib.c vs what clang lays out;
     - check mrustc's ALIGNOF/size macro + its niche/Option layout under target_os=wasi;
     - or build core WITHOUT --cfg debug_assertions (minicargo adds it; try a release std);
     - or try os-name="linux" in the spec (std still picks unix backend then → POSIX deps,
       back to the thread/socket shims — a different tradeoff to weigh);
     - or bump to MRUSTC_TARGET_VER=1.74 (newer std/mrustc may lay out cleanly).
  STATUS unchanged on the headline: no_std Rust + C + Zig fully work in-sandbox (merged).
  Full-std is a real deep grind here, not a quick win.

* 2026-06-07 (iter 13) — ★ ENTIRE std DEP GRAPH builds for wasi; blocked only on `wasi` crate
  Two fixes cracked the layout/asm walls together:
  - spec arch="wasm32" (ARCH_WASM32) → NO ARM barrier asm (target_arch=wasm32).
  - spec [backend.c] emulate-i128 = true → mrustc emits emulated i128 (struct{lo,hi}, align 8),
    fixing the OptionSome size/align asserts (native __int128 was align 16 ≠ mrustc's layout).
  - CC_wasm32_wasi=<wrapper> forces mrustc to cross-compile its C via wasi-sdk clang.
  RESULT: minicargo built the FULL std dependency graph for wasi (all .hir): core, alloc,
  compiler_builtins, cfg_if, dlmalloc, hashbrown, libc, panic_abort, panic_unwind,
  rustc_demangle, std_detect, unwind. std ITSELF then compiles its sys/wasi + os/wasi
  backend (os=wasi selection works!) and fails at ONE point:
    library/std/src/os/wasi/fs.rs:454 — `wasi::FILETYPE_BLOCK_DEVICE` → "Couldn't find path
    component 'wasi'": the external `wasi` bindings crate wasn't built as a std dependency.
  WHY: std's Cargo.toml lists `wasi` under [target.'cfg(target_os="wasi")'.dependencies];
  minicargo's target-dep cfg resolution didn't include it (the makefile's RUSTC_ARCH derivation
  defaulted to x86_64 since MRUSTC_TARGET is a file path → STD_ENV_ARCH=x86_64). std's MODULE
  cfg (mrustc, via spec os=wasi) correctly picked the wasi modules, but the CRATE-dep cfg
  (minicargo) didn't add the wasi crate.
  NEXT: get minicargo to build the vendored `wasi` crate as a std dep — fix its target cfg
  eval (pass the right arch/target so cfg(target_os="wasi") deps resolve), or build the wasi
  crate explicitly and -L it. Then std links; then hello.rs (println) in-sandbox → run.
  We are ~1 dep + final link from full std. Memory fine throughout.

* 2026-06-07 (iter 14) — ★★★ FULL STD: `println!` Rust runs in the wasm sandbox ★★★
  OUTPUT: "HELLO FROM RUST STD IN WASM: 42" — `fn main(){ println!(...) }` compiled through
  mrustc→libstd→C→clang.wasm→wasm, run under wasmtime. Real Rust + the real std library.
  The walls solved this run (all in std/mrustc-wasm-std.patch + std/build-libstd.sh):
  1. emulate-i128=true (spec) — emulated i128 struct (align 8) fixes the size/align asserts.
  2. ARCH_WASM32 (mrustc patch) — target_arch=wasm32, NO ARM barrier asm.
  3. built-in "wasm32-wasi" target (mrustc patch) — so --target wasm32-wasi works for mrustc
     AND matches std's [target.wasm32-wasi.dependencies] in minicargo.
  4. #[link(wasm_import_module)] handling (mrustc patch) — the `wasi` bindings crate uses it;
     mrustc errored before, now ignores it (imports resolve at link via wasi-libc/shim).
  5. STD_ENV_ARCH=wasm32 — std's env!() needs it.
  6. The `wasi` crate + libstd: minicargo's target-dep resolution didn't auto-add `wasi`, so
     build the wasi crate + libstd explicitly with --extern wasi (build-libstd.sh).
  7. Link: clang+wasm-ld with -lsetjmp (provides __wasm_setjmp/longjmp/__c_longjmp sjlj
     runtime) + a 1-line _Unwind_Resume stub (ustub.c; dead for non-panicking code; sjlj EH
     model ≠ Rust's _Unwind_*).
  Memory: libstd.c is 26MB; whole build stayed well under the wasm32 4GB ceiling.
  REMAINING for UNTRUSTED-in-sandbox std: the user hello.rs→C step here used NATIVE mrustc;
  rebuild mrustc.wasm WITH this patch (ARCH_WASM32 + builtin target + wasm_import) so untrusted
  .rs compiles in-sandbox against the (trusted, prebuilt) libstd — mechanical, same as no_std.
