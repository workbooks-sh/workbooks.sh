#+TITLE: Rust-in-WASM lane — build state & how to regenerate
#+NOTE: Read this FIRST in any session before deciding "what's built". Don't re-derive.

* The rule
  The RECIPE (scripts + PORT-LOG.org + std-build-plan.txt) is the source of truth and IS
  committed. The heavy artifacts below are GITIGNORED on purpose (≈2 GB, platform-specific,
  regenerable). Losing them costs build time, never knowledge. One command rebuilds them all:

  : bash compilers/rust/provision-rust.sh        # idempotent; fast-skips what exists

* Artifacts (local-only, gitignored) and what regenerates each
  | Artifact                                   | Size  | Regenerated by                       | ~time |
  |--------------------------------------------+-------+--------------------------------------+-------|
  | mrustc-root/mrustc/rustc-1.54.0-src        | 1.2 G | mrustc Makefile (RUSTCSRC target)    | ~3 m  |
  | mrustc-root/wasi-sdk-33.0-*                 | 616 M | build.sh (download)                  | ~2 m  |
  | ../clang/clang-root (sysroot+llvm.core)     | 104 M | ../clang/build.sh                    | ~2 m  |
  | mrustc-root/mrustc.wasm, mrustc_std.wasm    | 16 M  | build.sh (+ std patch for _std)      | ~6 m  |
  | mrustc-root/mrustc/bin/{mrustc,minicargo}   | 28 M  | make + make -f minicargo.mk          | ~8 m  |
  | mrustc-root/mrustc/output-wasi (libstd)     | 27 M  | std/prebuild-libstd.sh               | ~15 m |

* What's REPRODUCIBLE today (verified 2026-06-07 iter 25) vs proven-once
  REPRODUCIBLE from committed recipe: C, Zig, Rust **no_std** (e2e.sh). The 16 libstd dep
  crates also build cleanly via mrustc.wasm (consistent __2).
  ⚠️ Rust **full std**: proven ONCE (iter 24, "sum=55") but does NOT currently rebuild — the
  committed std-build-plan.txt libstd line makes mrustc.wasm infinite-recurse in
  Resolve_Absolute_Mod (PORT-LOG iter 25). The winning iter-24 config wasn't fully captured.
  Full-std is GATED until that mrustc.wasm recursion is debugged (Path A: -g mrustc.wasm) or
  the missing iter-24 cfg is rediscovered. Don't claim full-std reproducible until libstd
  actually emits from prebuild-libstd.sh.
  The two ALREADY-solved walls (still valid): (1) hash/ABI consistency → build libstd BY
  mrustc.wasm (__2); (2) abort_bitcast signext → clang.wasm -Xclang -disable-llvm-verifier.

* Wiring into PackageManager (wb-fm0) — the SEPARATE, ongoing task
  Compilers are done; fm0 only routes PackageManager.compile/3 + build_dir/2 away from native
  toolchains to Workbooks.Compilers. Status: C ✓ (fm0.1), Zig ✓ (fm0.2), Rust in progress (fm0.3).

* NEVER lose this again
  - Commit recipe/code changes promptly; `git push` before ANY `git reset`.
  - Do NOT `git reset --hard origin/main` with unpushed local commits (this discarded the
    ★ commits visible in the reflog). If unsure, `git push` first.
  - Artifacts don't need committing — `provision-rust.sh` rebuilds them idempotently.
