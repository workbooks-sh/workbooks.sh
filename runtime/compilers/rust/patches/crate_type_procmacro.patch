diff --git a/src/expand/crate_tags.cpp b/src/expand/crate_tags.cpp
index bfb346d3..8f0e8677 100644
--- a/src/expand/crate_tags.cpp
+++ b/src/expand/crate_tags.cpp
@@ -25,6 +25,11 @@ public:
         else if( name == "cdylib" ) {
             crate.m_crate_type = AST::Crate::Type::CDylib;
         }
+        else if( name == "proc-macro" ) {
+            // wb-v3d: many older proc-macro crates (derive-new, …) declare this in-source rather
+            // than only via Cargo's [lib] proc-macro=true / the --crate-type flag.
+            crate.m_crate_type = AST::Crate::Type::ProcMacro;
+        }
         else {
             ERROR(sp, E0000, "Unknown crate type '" << name << "'");
         }
