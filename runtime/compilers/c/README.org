#+TITLE: compilers/c — C compiler in WASM
#+STATUS: PLAN (P1)

Goal: compile untrusted .c → .wasm with the compiler running IN the sandbox.
Backend: none (tcc/chibicc have their own codegen — no LLVM).
Status: the c4 spike already ran a real C compiler in-sandbox (compile+run C, zero
native exec). This dir productionizes it with TinyCC (tcc) or chibicc.

Plan:
- Pin tcc (or chibicc) source + sha; build.sh compiles it to wasm32-wasi (wasi-sdk),
  stubbing unsupported libc (the tmpnam/clock class — cf. ../../toolkits/palette/runtimes/lua).
- Register via the `compiler:` framework; preopen the source dir; emit + run the .wasm.
- Prove: a real untrusted .c compiles in-sandbox and the output runs.
Frontier risk: low (tcc is small, self-contained). Closest to done.
