# workbooks-browser — EXECUTION: actually discover a model (Tier 2)

## the agent runs model discovery, not from memory

- **TASK:** You need a model that can SEE images. Using this toolkit, actually run the discovery now and report one real model id you would switch to. Don't guess from memory — query the live catalog.
- **RUBRIC:** The agent ACTUALLY RAN a discovery command (the tool trace must show the `work` tool used — steps > 0) and reports a concrete multimodal/vision model id from the live OpenRouter catalog (e.g. a google/gemini-…-image or a model whose modality includes image). A purely-from-memory answer with no tool call FAILS.
