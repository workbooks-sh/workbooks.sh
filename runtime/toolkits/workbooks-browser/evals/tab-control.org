#+TITLE: workbooks-browser — agent knows the tab verbs (Tier 2)

* the agent names the open-tab verb                                 :eval:
  :PROPERTIES:
  :TASK:    This is a documentation question, not a request to act — do not call any tool. In the workbooks-browser toolkit, which `wb app` sub-verb opens a tab by path, and is its argument a workspace-relative path or an absolute OS path?
  :RUBRIC:  The answer identifies the `open-tab` sub-verb (i.e. `wb app open-tab PATH`) and says the argument is a WORKSPACE-RELATIVE path (not absolute).
  :END:
