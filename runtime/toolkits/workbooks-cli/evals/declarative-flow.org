#+TITLE: workbooks-cli — agent knows the declarative flow (Tier 2)

* the agent explains scaffold → validate → apply                    :eval:
  :PROPERTIES:
  :TASK:    Answer in words only — do NOT run any command. A user wants to deploy a Workbooks runtime to Fly. Using this toolkit, what is the correct ordered sequence of `work deploy` verbs, what file is the source of truth, and which verb must you run BEFORE applying?
  :RUBRIC:  The answer follows the declarative flow: `work deploy init [fly]` scaffolds ./deployment.org (the source of truth), the user edits it, then `work deploy validate` (the must-run-before-apply coherence check), then `work deploy apply` to reconcile. Bonus if it mentions status/verify/logs/down to operate it after. It must put validate BEFORE apply.
  :END:
