#+TITLE: text toolkit
#+TOOLKIT: text
#+VERSION: 0.1.0
#+CLI_BIN: upper
#+CLI_VERSION_RANGE: >=0.1
#+STATUS: stable
#+TAGLINE: String transforms — case/normalize text as a sandboxed WASM command.

* text — string transforms                                   :toolkit:
  :PROPERTIES:
  :ID:        text
  :CLI_BIN:   upper
  :STATUS:    stable
  :SKILL_DIR: skills/
  :END:

  Transform text the agent tends to get wrong from training priors. The wrapped
  CLI =upper= runs as a *sandboxed WASM command* (CommandRegistry / =run-command=)
  — not a native PATH binary — but the manifest + skill shape is the canonical
  toolkit format (manifest indexes skills by NEED; skills hold the recipes).

  Skill index (read on demand):

  | Skill      | Use when                              |
  |------------+---------------------------------------|
  | =overview= | First contact — what this toolkit does|
  | =uppercase=| Shout / normalize a string to caps    |
