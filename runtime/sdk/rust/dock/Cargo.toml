[package]
name = "dock"
version = "0.1.0"
edition = "2021"
description = "Dock SDK — ergonomic, cap-scoped bindings for workbooks:engine toolkit authors (epic wb-rhs, wb-rhs.3)."
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
