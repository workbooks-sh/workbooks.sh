#+TITLE: Welcome to Workbooks

* What is a Workbook?

A *Workbook* is a single Org file that the runtime renders, runs, and ships.
There is no "document vs notebook vs app" — the /content/ decides. Write Org,
and you get a clean document; add a ~:component:~ block, and it becomes live.

** Why it's different

- One file, rendered anywhere — browser or server
- Real compute: source blocks compile to WASM
- Versioned by =git=, shared over Radicle

You can link to things like [[https://orgmode.org][Org mode]] inline, and the
renderer handles *emphasis*, /italics/, =code=, and ~verbatim~ cleanly.

** A small table

| Layer    | Tool    | License |
|----------+---------+---------|
| Render   | orgize  | MIT     |
| Runtime  | Wasmex  | MIT     |
| Federate | Radicle | MIT     |

#+begin_quote
The more we let content be self-describing, the more system features dissolve
into just content.
#+end_quote
