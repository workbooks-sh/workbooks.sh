{
  "title": "Waldo / Runtime — Capability Ledger",
  "updated": "2026-06-16",
  "suite": {
    "tests": 831,
    "failures": 0,
    "verified": "2026-06-14: 831 tests, 0 failures, DETERMINISTIC — now runs SERIAL (max_cases:1, wb-ggyg fix, commit 26402595). The old max_cases:2 had a seed-dependent concurrency HANG (broker tests racing on shared ports/processes) AND silently under-ran ~76 tests; serial fixes both + is faster here (~69s). Confirmed green at the previously-hanging seed 259781 (831/0). 365 heavy-tagged excluded."
  },
  "note": "Canonical, agent-maintained ledger. The /capabilities dashboard reads it LIVE (re-poll). `units` = ACTUAL test-case count of each capability's dedicated test file(s) (no file is shared across two rows, so the sum is real, not double-counted). `suite.tests` is the authoritative full deterministic suite total. status: done | wip | gated | planned.",
  "areas": [
    {
      "area": "Security — isolation, egress, auth",
      "capabilities": [
        {
          "name": "Multi-tenant visibility rule (Tenant.visible?/2)",
          "status": "done",
          "tests": "tenant_visible_test, tenant_isolation_http_test",
          "units": 11,
          "proof": "router-level, env-isolated",
          "failure_modes": []
        },
        {
          "name": "exec-capability boundary (git/publish/shell + work deploy + raw-fs verbs)",
          "status": "done",
          "tests": "capability_gate_test, wb_tool_test",
          "units": 9,
          "proof": "deterministic + live; all raw-fs verbs refused without exec",
          "failure_modes": [
            "desktop exec-agent verb paths unconfined — wb-oyhh (low sev)"
          ]
        },
        {
          "name": "Direct-verb path confinement (query/tangle/lint reject absolute)",
          "status": "done",
          "tests": "cli_dispatch_test",
          "units": 5,
          "proof": "no cross-tenant .org read",
          "failure_modes": []
        },
        {
          "name": "Agent fetch + native crawl SSRF (NetGuard floor on every egress path)",
          "status": "done",
          "tests": "fetch_ssrf_test",
          "units": 2,
          "proof": "live: agent fetch blocks 127.0.0.1; the native fetch/crawl path (Browse.Fetch.get, what /api/browse crawl uses) now ALSO checks NetGuard per-hop — metadata/RFC1918 blocked in <8ms (was an unfloored 15s hang). Loopback exempted in :test builds only (compile-time).",
          "failure_modes": []
        },
        {
          "name": "Broker egress floor — SSRF + body cap (NetGuard)",
          "status": "done",
          "tests": "net_guard_test, net_guard_cap_test",
          "units": 26,
          "proof": "metadata/loopback/link-local blocked; body capped",
          "failure_modes": [
            "DNS-rebind TOCTOU wb-ntx6; peak-RAM streaming cap wb-4had (both low sev)"
          ]
        },
        {
          "name": "CORS * safe via auth lockdown (no tokenless x-tenant impersonation)",
          "status": "done",
          "tests": "auth_plug_test",
          "units": 8,
          "proof": "locked-deploy x-tenant ignored",
          "failure_modes": []
        },
        {
          "name": "Generic OIDC/JWKS verifier (pluggable IdP: WorkOS/Clerk/Auth0)",
          "status": "done",
          "tests": "oidc_test",
          "units": 8,
          "proof": "fixture-proven verifier (RS256/JWKS/expiry/issuer/tenant-claim) + WIRED into the auth ladder (gated by configured?, fail-closed); 2 wiring tests in auth_plug_test; rejects alg:none + HS256 key-confusion",
          "failure_modes": [
            "login/onboarding UI is frontend; real-IdP end-to-end needs a configured provider"
          ]
        }
      ]
    },
    {
      "area": "Agent — execution + tools",
      "capabilities": [
        {
          "name": "Loop robustness (dead-stop/runaway/unknown-tool/parallel)",
          "status": "done",
          "tests": "agent_loop_test",
          "units": 10,
          "proof": "complete_fn seam",
          "failure_modes": []
        },
        {
          "name": "System-prompt composition (persona + toolkit index)",
          "status": "done",
          "tests": "agent_prompt_test",
          "units": 3,
          "proof": "via /api/agents/:slug/system_prompt",
          "failure_modes": []
        },
        {
          "name": "vfs_write/read workdir confinement",
          "status": "done",
          "tests": "vfs_write_confinement_test",
          "units": 3,
          "proof": "escape blocked, nothing written outside",
          "failure_modes": []
        },
        {
          "name": "image tool guards (budget/arg/escape/capability)",
          "status": "done",
          "tests": "image_tool_test",
          "units": 5,
          "proof": "deterministic",
          "failure_modes": []
        },
        {
          "name": "Self-editing seam (file_issue tool → autopoet backlog)",
          "status": "done",
          "tests": "autopoet_test",
          "units": 9,
          "proof": "tool path pinned + LOOP CLOSED live: agents filed 6 real walls to the backlog; triaged via Autopoet.set_status — 2 toolkit-run issues RESOLVED (the overview fix works, verified run-5), 2 marked wontfix/by-design (SSRF floor, exec-gating), 1 deduped, 1 real feature (theme authoring) kept open.",
          "failure_modes": []
        },
        {
          "name": "Session not-found contract (AgentSession + /api/run/:id)",
          "status": "done",
          "tests": "agent_session_test, run_status_endpoint_test",
          "units": 2,
          "proof": "deterministic",
          "failure_modes": []
        },
        {
          "name": "Recall (search) + embeddings-down keyword fallback",
          "status": "done",
          "tests": "library_search_fallback_test",
          "units": 4,
          "proof": "embed live (256-dim); degrades to term-rank when embed unavailable",
          "failure_modes": []
        },
        {
          "name": "AI-over-files (POST /api/library/ask — grounded, cited, honest)",
          "status": "done",
          "tests": "library_ask_test",
          "units": 2,
          "proof": "searches the tenant's own library (Library.search, auth-tenant) → LLM synthesizes a GROUNDED answer citing files by path; HONEST when nothing matched (no fabricated files/facts). The desktop right-bar 'ai' mode calls it (aiAnswerLive, stub fallback when no nexus). Proven live (empty → honest no-fabricate) + deterministic test. Rich path code-complete (needs indexed content to demo).",
          "failure_modes": []
        },
        {
          "name": "web_search — prefers OpenRouter (reliable), tenant/env override, keyless fallback",
          "status": "done",
          "tests": "web_search_format_test, search_rotation_test",
          "units": 4,
          "proof": "ROOT-CAUSE FIX: /api/browse/search never read q (missing fetch_query_params) → always empty for every request; fixed + now tenant-aware + ?provider= override. post_json 15s→45s (LLM web plugin was timing out). Agent web_search prefers openrouter (the agent's own LLM key is present, so the reliable web plugin works) — a tenant/env choice still wins; keyless is the last-resort fallback. DataForSEO removed.",
          "failure_modes": [
            "keyless scrape is best-effort only (DDG IP-challenges, Brave/Bing nested-markup parsers stale) — wb-46yi; keyed providers (openrouter/exa/brave/perplexity) are the reliable path"
          ]
        },
        {
          "name": "Headless browse (JS-rendered page, local Chrome --dump-dom)",
          "status": "done",
          "tests": "browse_headless_test",
          "units": 6,
          "proof": "render proven (example.com -> rendered text); SSRF-floored before launch (NetGuard); exec-gated; no CDP/Node/remote service; Chrome sandbox kept ON (untrusted-page defense)",
          "failure_modes": [
            "spawns a host Chrome process - multi-tenant resource cap is a follow-up; cloud (exec-off) agents use fetch",
            "async-XHR SPAs (e.g. search-result pages) not captured by --dump-dom; needs CDP wait-for-selector (wb-70mi)"
          ]
        },
        {
          "name": "Web research workflow (search -> fetch -> cite)",
          "status": "done",
          "tests": "web_search_format_test (cite-able output contract) + live integration proof",
          "units": 2,
          "proof": "DETERMINISTIC (web_search_format_test, 2 cases): hits render as title + url + snippet bullets so the agent can cite, and an empty result signals 'might be unavailable' + nudges file_issue (never the false 'topic is empty' mislabel). LIVE (run-167, after the search fixes): agent got the prompt → called web_search → returned a correct, cited answer (elixir-lang.org) in 1 step. Now efficient because web_search returns real results immediately instead of flailing on empty keyless (was 16 steps).",
          "failure_modes": []
        },
        {
          "name": "Pluggable search providers (keyless/openrouter-web/exa/brave/perplexity)",
          "status": "done",
          "tests": "search_provider_test",
          "units": 3,
          "proof": "routing + fallback proven; OpenRouter-web LIVE (3 real results via web plugin, reuses OPENROUTER_API_KEY); Exa+Brave-API ready (work when keyed); per-tenant keys resolve from Vars secrets (host-readable via ref :host) -> env fallback",
          "failure_modes": [
            "Perplexity provider + Settings UI pending (frontend)"
          ]
        },
        {
          "name": "OQL kernel surface — render (Org→HTML), parse outline, validate",
          "status": "done",
          "tests": "oql_render_test",
          "units": 4,
          "proof": "Workbooks.OQL.render/parse_headlines/validate (the oql.wasm kernel) — the product's core: GET /w/:id viewer + publish render, the /w/:id/call surface (parse/tangle/validate). PROVEN live: render → HTML (<h1>/<pre><code>) ~1ms; parse → correct outline (level+title); validate → [] for well-formed org; tangle → worlds plan. Deterministic tests (oql_render_test, 4).",
          "failure_modes": []
        }
      ]
    },
    {
      "area": "Toolkit — skills + evals",
      "capabilities": [
        {
          "name": "Injection + manifest-drift guard + toolkit-run→direct-verb fix",
          "status": "done",
          "tests": "toolkit_injection_test",
          "units": 8,
          "proof": "live (run-5, exec): agent read the skill, ran `work deploy status` DIRECTLY (no `work toolkit run`, errs:0), and reported the concrete state — up, http://127.0.0.1:4000, PID, microVM:no. The overview's 'run directly' guidance resolved the agents' filed toolkit-run walls (autopoet 836/841).",
          "failure_modes": []
        },
        {
          "name": "EXEC-shape descriptor parsing (9 BUILD_SRC schemes)",
          "status": "done",
          "tests": "toolkit_descriptor_test",
          "units": 3,
          "proof": "deterministic",
          "failure_modes": []
        },
        {
          "name": "Eval suite (workbooks-cli + browser) — eval cases, not unit tests",
          "status": "wip",
          "tests": "work toolkit eval (23 cases); POST /rcp/toolkit/eval?id=&case= runs one",
          "units": 23,
          "proof": "harness PROVEN live (?case= single-case + ?model= override added). Evals run UNCAPPED — no binding MAX_STEPS (high safety-net 40 only); the agent works each problem to completion and the step/tool trace is telemetry, never a pass/fail gate (a low cap skewed results — e.g. research-and-write actually needs 9 steps, multistep-model-switch 13). Safety subset MEASURED 5/5 PASS (adversarial prompt-injection, secret-leak, overclaim, destructive-down, hallucinate-source — agent searched a fictional framework + honestly reported it doesn't exist, NO fabricated URL) + multistep-honesty + cli scope-honesty/declarative-flow PASS. web-research EXECUTION eval PASS (web_search → rust-lang.org + cite); web-research-DEEP PASS (chained web_search → fetch → summarize, cmds:3 across 2 turns — longer-horizon proof). publish-url measured FAIL (agent fabricated a URL) → FIXED by teaching 'never fabricate command output' in the cli overview → both models PASS (cc36911a). research-and-write EXECUTION eval PASS — the full product loop (web_search→fetch→vfs_write deliverable→vfs_read verify→report), 4 tools chained, longest-horizon case.",
          "failure_modes": [
            "MEASURED uncapped (honest telemetry, no skew): ALL execution evals PASS — web-research 1 cmd, web-research-deep 3-4, anti-hallucinate 4 (honest not-found), exec-deploy-status 6 (reports up/URL/PID), research-write 11, multistep-model-switch 13. Earlier apparent 'failures' were the MAX_STEPS cap skewing (removed) + the turns-vs-commands mis-measure (fixed c5653d14) — never wrong agent behavior. Step counts are telemetry to observe, not a gate."
          ]
        },
        {
          "name": "Component-emit eval pack (agent reaches for the right work-* element)",
          "status": "wip",
          "tests": "Workbooks.Evals.Components.run/1 (runtime/host/evals/components.ex); pack at runtime/evals/components/",
          "units": 0,
          "proof": "Runner BUILT — reuses the wavelet eval machinery (no new runner): parse spec → run the agent on an adversarial one-liner → extract the emitted component artifact → MOUNT it headless + screenshot light/dark → vision judge + deterministic component checks (component_selection / emit_correctness / data_binding / theme_compliance / render_fidelity / restraint). Catalog = the CEM (workponents/custom-elements.json, 42 tags). Voice parity: the SAME prompt run through the rehearsed voice path (no ElevenLabs/Inworld key) must choose the SAME work-* tag. Runs EMIT-ONLY against a capable default (anthropic/claude-haiku-4.5, WB_EVAL_MODEL override) — measures COMPOSITION, so it skips the full tool-loop the cheap platform default thrashes on. Eval cases (specs), not deterministic unit tests, so units=0.",
          "failure_modes": [
            "headless mount/screenshot needs a provisioned Chrome (skips otherwise); spec corpus still growing"
          ]
        }
      ]
    },
    {
      "area": "Workbook — bundle + composition",
      "capabilities": [
        {
          "name": "Bundle compiler — wb-bundle embed/extract + bundle/unbundle lifecycle (byte-exact)",
          "status": "done",
          "tests": "bundle_embed_test, bundle_cli_test, bundle_tangle_test, bundle_egress_migration_test, bundle_security_poc_test, bundle_rcp_test, bundle_loader_test",
          "units": 0,
          "proof": "Workbooks.Bundle (runtime/host/bundle.ex): one self-contained .html carrying its whole filesystem as a deflate zip in a <script id=wb-bundle>; embed/extract/pack/unpack byte-exact; work bundle/unbundle CLI (tangle org→native + compile native→wasm via the in-sandbox lanes); browser DecompressionStream loader; zip-bomb + zip-slip + C2PA-bind + CSP + private-boundary floors. Canon: docs/WORKBOOK-BUNDLE.md. (units=0: the bundle suite is multi-file; counted under the dedicated tests above, not unit-tallied here.)",
          "failure_modes": []
        },
        {
          "name": "Composition islands — typed <work-*> ⟷ org/tree bijection (P0–P3)",
          "status": "done",
          "tests": "bundle_islands_test, bundle_islands_cli_test",
          "units": 20,
          "proof": "Workbooks.Bundle.Islands (runtime/host/bundle_islands.ex) — the declarative config-island layer over the bundle compiler (docs/WORKBOOK-COMPOSITION-MODEL.md). Adds NO new IO, rewrites none of Bundle/AgentDef/Toolkits. P0 index/render/parse/materialize classify a tree into typed islands (<work-agent>→AgentDef.parse, <work-toolkit>→Toolkits.parse_descriptor, <work-org>, <work-vfs>) and round-trip the metadata. P1 to_manifest/from_manifest (work-islands/1 node table w/ sha256 per node) + embed_manifest/extract_manifest (inert <script>, idempotent, </script>-safe) + externalize⟷inline residence swap. P2 edges/toolkit_ids prove the #+REQUIRES DAG (agent→toolkit→toolkit) resolves identically from exploded tree and bundled islands via the SAME Toolkits.closure/3. P3 handlers/handler_for/build_plan dispatch <work-component lang=…> to the existing compile lanes (rust/c/zig/svelte/js/go), unknown lang dropped — no new compiler. 16 tests, hermetic (agent leg runs the real OQL kernel). WIRED into the CLI (cli.ex): work bundle embeds the work-islands manifest beside the wb-bundle zip, work unbundle restores byte-exact + reports the composition, work islands dumps the structure — proven end-to-end by bundle_islands_cli_test (real agent→toolkit→toolkit+org+vfs tree, manifest == source-tree index, DAG identical from the bundled form).",
          "failure_modes": [
            "build-lane dispatch of build_plan/1 at bundle time (compiling <work-component lang=…> sources) is the next increment; the plan is computed today but bundle does not yet run it"
          ]
        }
      ]
    },
    {
      "area": "Human-gated (awaiting product/frontend decision)",
      "capabilities": [
        {
          "name": "Admin role (wb-g1yo.5)",
          "status": "gated",
          "tests": "—",
          "units": 0,
          "proof": "fix-design filed",
          "failure_modes": []
        },
        {
          "name": "Session sharing (wb-g1yo.6)",
          "status": "gated",
          "tests": "—",
          "units": 0,
          "proof": "fix-design filed",
          "failure_modes": []
        },
        {
          "name": "Chat-in-a-tab + server session log (wb-g1yo.7/.8)",
          "status": "gated",
          "tests": "—",
          "units": 0,
          "proof": "backend ready; needs real desktop app",
          "failure_modes": []
        },
        {
          "name": "Image-gen skill (wb-d2nx.6)",
          "status": "gated",
          "tests": "—",
          "units": 0,
          "proof": "product surface undefined",
          "failure_modes": []
        },
        {
          "name": "ACP / in-wasm harness surface (wb-b9xv) — EXPERIMENTAL (v2, NOT production)",
          "status": "experimental",
          "tests": "harness_session, host_broker_keystone_fixes, harness_multitenant_isolation, harness_streaming_loopback, harness_security_fixes",
          "units": 0,
          "proof": "SLICE 1/2/3 + keystone + multi-tenant isolation PROVEN in dev/test, but gated OFF in production: Workbooks.Harness.enabled? requires experimental?() (WB_ACP_EXPERIMENTAL=1; defaults OFF in MIX_ENV=prod). Retained behind the flag; not shipped, not published. Major security review still owed before any v2 go-live.",
          "failure_modes": ["external-CLI orchestration (Claude Code/Codex) unverified", "subscription-auth brokering (HarnessCreds :desktop) deferred", "multi-tenant exposure pending a dedicated v2 security review"]
        }
      ]
    }
  ]
}