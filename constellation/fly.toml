app = "ether-granite-bench"
primary_region = "iad"

[build]
  dockerfile = "Dockerfile"

# Realistic per-invocation size: 4 vCPU + 4GB holds the 2GB Granite-3B-Q4 + KV with headroom.
[[vm]]
  size = "shared-cpu-4x"
  memory = "4096"
