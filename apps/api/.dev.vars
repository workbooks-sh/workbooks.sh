# brandnana API — local dev secrets (GITIGNORED; do NOT commit).
# Provided by Shane in-session 2026-06-03.
# Push one secret:   (run inside apps/api)
#   bunx wrangler secret put <NAME>
# Push all (additive bulk upsert):
#   bun run secrets:push
#
# NOTE: prod already has CONTEXT_DEV_API_KEY, FIRECRAWL_API_KEY,
# OPENROUTER_API_KEY, CEREBRAS_API_KEY, CLERK_SECRET_KEY, GROQ_API_KEY,
# THE_COMPANIES_API_KEY, WORKOS_API_KEY, WORKOS_CLIENT_ID (verified via
# `wrangler secret list`). Those values are NOT mirrored here.

# ScrapeCreators — powers ~155 social verbs + ads search.
SCRAPECREATORS_API_KEY="Lw9aSzjU16MTU63LLogtkv0N3vs1"

# Valyu (valyu.network) — web/deepsearch + curated datasets. Replaces Exa as
# the search/discovery provider. Key format val_*. Code consumer pending
# (search-provider abstraction; wire valyu.ts mirroring scrape/exa.ts).
VALYU_API_KEY="val_ed415afff9ff6bded3ff002f3111d8ec03a52fffe199ac0680adb0c83ac2a2cf"
